// generated by github.com/moontrade/dtc-go/codegen/go at 2022-05-27 08:32:53.747629 +0800 WITA m=+0.008238335
use super::*;

pub(crate) const TRADING_SYMBOL_STATUS_FIXED_SIZE: usize = 12;

/// size       u16                = TradingSymbolStatusFixedSize  (12)
/// type       u16                = TRADING_SYMBOL_STATUS  (138)
/// symbol_id  u32                = 0
/// status     TradingStatusEnum  = TRADING_STATUS_UNKNOWN  (0)
pub(crate) const TRADING_SYMBOL_STATUS_FIXED_DEFAULT: [u8; 12] =
    [12, 0, 138, 0, 0, 0, 0, 0, 0, 0, 0, 0];

/// Sent by the Server to the Client to indicate the status of the symbol
/// in regards to whether trading is open or closed or some other intermediate
/// state.
pub trait TradingSymbolStatus: Message {
    type Safe: TradingSymbolStatus;
    type Unsafe: TradingSymbolStatus;

    /// This is the same SymbolID sent by the Client in the MarketDataRequestVLS
    /// message which corresponds to the Symbol that the status in this message
    /// is for.
    fn symbol_id(&self) -> u32;

    /// The current trading status for the symbol. Can be one of the following.
    /// The current trading status for the symbol. Can be one of the following.
    ///
    /// TRADING_STATUS_UNKNOWN = 0
    /// TRADING_STATUS_PRE_OPEN = 1
    /// TRADING_STATUS_OPEN = 2
    /// TRADING_STATUS_CLOSE = 3
    /// TRADING_STATUS_TRADING_HALT = 4
    fn status(&self) -> TradingStatusEnum;

    /// This is the same SymbolID sent by the Client in the MarketDataRequestVLS
    /// message which corresponds to the Symbol that the status in this message
    /// is for.
    fn set_symbol_id(&mut self, value: u32) -> &mut Self;

    /// The current trading status for the symbol. Can be one of the following.
    /// The current trading status for the symbol. Can be one of the following.
    ///
    /// TRADING_STATUS_UNKNOWN = 0
    /// TRADING_STATUS_PRE_OPEN = 1
    /// TRADING_STATUS_OPEN = 2
    /// TRADING_STATUS_CLOSE = 3
    /// TRADING_STATUS_TRADING_HALT = 4
    fn set_status(&mut self, value: TradingStatusEnum) -> &mut Self;

    fn clone_safe(&self) -> Self::Safe;

    fn to_safe(self) -> Self::Safe;

    fn copy_to(&self, to: &mut impl TradingSymbolStatus) {
        to.set_symbol_id(self.symbol_id());
        to.set_status(self.status());
    }

    #[inline]
    fn parse<F: Fn(Parsed<Self::Safe, Self::Unsafe>) -> Result<(), crate::Error>>(
        data: &[u8],
        f: F,
    ) -> Result<(), crate::Error> {
        if data.len() < 4 {
            return Err(crate::Error::Malformed("need more data"));
        }
        let size = unsafe { u16::from_le(*(data.as_ptr() as *const u16)) };
        let base_size = if Self::BASE_SIZE_OFFSET == 0 {
            size
        } else {
            let base_size = unsafe {
                u16::from_le(*(data.as_ptr().offset(Self::BASE_SIZE_OFFSET) as *const u16))
            };
            if base_size > size {
                return Err(crate::Error::Malformed("base_size is greater than size"));
            }
            base_size
        };
        if (base_size as usize) >= Self::BASE_SIZE {
            let msg = unsafe { Self::Safe::from_raw_parts(data.as_ptr(), size as usize) };
            let r = f(Parsed::Safe(&msg));
            core::mem::forget(msg);
            r
        } else {
            let msg = unsafe { Self::Unsafe::from_raw_parts(data.as_ptr(), size as usize) };
            let r = f(Parsed::Unsafe(&msg));
            core::mem::forget(msg);
            r
        }
    }
}

/// Sent by the Server to the Client to indicate the status of the symbol
/// in regards to whether trading is open or closed or some other intermediate
/// state.
pub struct TradingSymbolStatusFixed {
    data: *const TradingSymbolStatusFixedData,
}

pub struct TradingSymbolStatusFixedUnsafe {
    data: *const TradingSymbolStatusFixedData,
}

#[repr(packed(8), C)]
pub struct TradingSymbolStatusFixedData {
    size: u16,
    r#type: u16,
    symbol_id: u32,
    status: TradingStatusEnum,
}

impl TradingSymbolStatusFixedData {
    pub fn new() -> Self {
        Self {
            size: 12u16.to_le(),
            r#type: TRADING_SYMBOL_STATUS.to_le(),
            symbol_id: 0u32,
            status: TradingStatusEnum::TradingStatusUnknown.to_le(),
        }
    }
}

unsafe impl Send for TradingSymbolStatusFixed {}
unsafe impl Send for TradingSymbolStatusFixedUnsafe {}
unsafe impl Send for TradingSymbolStatusFixedData {}

impl Drop for TradingSymbolStatusFixed {
    #[inline]
    fn drop(&mut self) {
        crate::deallocate(self.data as *mut u8, self.capacity() as usize);
    }
}

impl Drop for TradingSymbolStatusFixedUnsafe {
    #[inline]
    fn drop(&mut self) {
        crate::deallocate(self.data as *mut u8, self.capacity() as usize);
    }
}

impl Clone for TradingSymbolStatusFixed {
    #[inline]
    fn clone(&self) -> Self {
        let mut c = Self::new();
        self.copy_to(&mut c);
        c
    }
}

impl Clone for TradingSymbolStatusFixedUnsafe {
    #[inline]
    fn clone(&self) -> Self {
        let mut c = Self::new();
        self.copy_to(&mut c);
        c
    }
}

impl Into<Vec<u8>> for TradingSymbolStatusFixed {
    #[inline]
    fn into(self) -> Vec<u8> {
        self.into_vec()
    }
}

impl Into<Vec<u8>> for TradingSymbolStatusFixedUnsafe {
    #[inline]
    fn into(self) -> Vec<u8> {
        self.into_vec()
    }
}

impl core::ops::Deref for TradingSymbolStatusFixed {
    type Target = TradingSymbolStatusFixedData;

    #[inline]
    fn deref(&self) -> &Self::Target {
        unsafe { &*self.data }
    }
}

impl core::ops::DerefMut for TradingSymbolStatusFixed {
    #[inline]
    fn deref_mut(&mut self) -> &mut Self::Target {
        unsafe { &mut *(self.data as *mut Self::Target) }
    }
}

impl core::ops::Deref for TradingSymbolStatusFixedUnsafe {
    type Target = TradingSymbolStatusFixedData;

    #[inline]
    fn deref(&self) -> &Self::Target {
        unsafe { &*self.data }
    }
}

impl core::ops::DerefMut for TradingSymbolStatusFixedUnsafe {
    #[inline]
    fn deref_mut(&mut self) -> &mut Self::Target {
        unsafe { &mut *(self.data as *mut Self::Target) }
    }
}

impl core::fmt::Display for TradingSymbolStatusFixed {
    #[inline]
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(
            format!(
                "TradingSymbolStatusFixed(size: {}, type: {}, symbol_id: {}, status: {})",
                self.size(),
                self.r#type(),
                self.symbol_id(),
                self.status()
            )
            .as_str(),
        )
    }
}

impl core::fmt::Debug for TradingSymbolStatusFixed {
    #[inline]
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(
            format!(
                "TradingSymbolStatusFixed(size: {}, type: {}, symbol_id: {}, status: {})",
                self.size(),
                self.r#type(),
                self.symbol_id(),
                self.status()
            )
            .as_str(),
        )
    }
}

impl core::fmt::Display for TradingSymbolStatusFixedUnsafe {
    #[inline]
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(
            format!(
                "TradingSymbolStatusFixedUnsafe(size: {}, type: {}, symbol_id: {}, status: {})",
                self.size(),
                self.r#type(),
                self.symbol_id(),
                self.status()
            )
            .as_str(),
        )
    }
}

impl core::fmt::Debug for TradingSymbolStatusFixedUnsafe {
    #[inline]
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(
            format!(
                "TradingSymbolStatusFixedUnsafe(size: {}, type: {}, symbol_id: {}, status: {})",
                self.size(),
                self.r#type(),
                self.symbol_id(),
                self.status()
            )
            .as_str(),
        )
    }
}

impl crate::Message for TradingSymbolStatusFixed {
    type Data = TradingSymbolStatusFixedData;

    const TYPE: u16 = TRADING_SYMBOL_STATUS;
    const BASE_SIZE: usize = 12;
    const BASE_SIZE_OFFSET: isize = 0;

    #[inline]
    fn new() -> Self {
        Self {
            data: crate::allocate(Self::BASE_SIZE, TradingSymbolStatusFixedData::new()),
        }
    }

    #[inline]
    fn size(&self) -> u16 {
        u16::from_le(self.size)
    }

    #[inline]
    fn r#type(&self) -> u16 {
        u16::from_le(self.r#type)
    }

    #[inline]
    fn base_size(&self) -> u16 {
        u16::from_le(self.size)
    }

    #[inline]
    fn capacity(&self) -> u16 {
        u16::from_le(self.size)
    }

    #[inline]
    unsafe fn as_ptr(&self) -> *const Self::Data {
        self.data
    }

    #[inline]
    unsafe fn from_raw_parts(data: *const u8, _: usize) -> Self {
        Self {
            data: data as *const TradingSymbolStatusFixedData,
        }
    }
}
impl crate::Message for TradingSymbolStatusFixedUnsafe {
    type Data = TradingSymbolStatusFixedData;

    const TYPE: u16 = TRADING_SYMBOL_STATUS;
    const BASE_SIZE: usize = 12;
    const BASE_SIZE_OFFSET: isize = 0;

    #[inline]
    fn new() -> Self {
        Self {
            data: crate::allocate(Self::BASE_SIZE, TradingSymbolStatusFixedData::new()),
        }
    }

    #[inline]
    fn size(&self) -> u16 {
        u16::from_le(self.size)
    }

    #[inline]
    fn r#type(&self) -> u16 {
        u16::from_le(self.r#type)
    }

    #[inline]
    fn base_size(&self) -> u16 {
        u16::from_le(self.size)
    }

    #[inline]
    fn capacity(&self) -> u16 {
        u16::from_le(self.size)
    }

    #[inline]
    unsafe fn as_ptr(&self) -> *const Self::Data {
        self.data
    }

    #[inline]
    unsafe fn from_raw_parts(data: *const u8, _: usize) -> Self {
        Self {
            data: data as *const TradingSymbolStatusFixedData,
        }
    }
}
/// Sent by the Server to the Client to indicate the status of the symbol
/// in regards to whether trading is open or closed or some other intermediate
/// state.
impl TradingSymbolStatus for TradingSymbolStatusFixed {
    type Safe = TradingSymbolStatusFixed;
    type Unsafe = TradingSymbolStatusFixedUnsafe;

    /// This is the same SymbolID sent by the Client in the MarketDataRequestVLS
    /// message which corresponds to the Symbol that the status in this message
    /// is for.
    fn symbol_id(&self) -> u32 {
        u32::from_le(self.symbol_id)
    }

    /// The current trading status for the symbol. Can be one of the following.
    /// The current trading status for the symbol. Can be one of the following.
    ///
    /// TRADING_STATUS_UNKNOWN = 0
    /// TRADING_STATUS_PRE_OPEN = 1
    /// TRADING_STATUS_OPEN = 2
    /// TRADING_STATUS_CLOSE = 3
    /// TRADING_STATUS_TRADING_HALT = 4
    fn status(&self) -> TradingStatusEnum {
        TradingStatusEnum::from_le(self.status)
    }

    /// This is the same SymbolID sent by the Client in the MarketDataRequestVLS
    /// message which corresponds to the Symbol that the status in this message
    /// is for.
    fn set_symbol_id(&mut self, value: u32) -> &mut Self {
        self.symbol_id = value.to_le();
        self
    }

    /// The current trading status for the symbol. Can be one of the following.
    /// The current trading status for the symbol. Can be one of the following.
    ///
    /// TRADING_STATUS_UNKNOWN = 0
    /// TRADING_STATUS_PRE_OPEN = 1
    /// TRADING_STATUS_OPEN = 2
    /// TRADING_STATUS_CLOSE = 3
    /// TRADING_STATUS_TRADING_HALT = 4
    fn set_status(&mut self, value: TradingStatusEnum) -> &mut Self {
        self.status = unsafe { core::mem::transmute((value as i8).to_le()) };
        self
    }

    #[inline]
    fn clone_safe(&self) -> Self::Safe {
        let mut s = Self::Safe::new();
        self.copy_to(&mut s);
        s
    }

    #[inline]
    fn to_safe(self) -> Self::Safe {
        self
    }
}

/// Sent by the Server to the Client to indicate the status of the symbol
/// in regards to whether trading is open or closed or some other intermediate
/// state.
impl TradingSymbolStatus for TradingSymbolStatusFixedUnsafe {
    type Safe = TradingSymbolStatusFixed;
    type Unsafe = TradingSymbolStatusFixedUnsafe;

    /// This is the same SymbolID sent by the Client in the MarketDataRequestVLS
    /// message which corresponds to the Symbol that the status in this message
    /// is for.
    fn symbol_id(&self) -> u32 {
        if self.is_out_of_bounds(8) {
            0u32
        } else {
            u32::from_le(self.symbol_id)
        }
    }

    /// The current trading status for the symbol. Can be one of the following.
    /// The current trading status for the symbol. Can be one of the following.
    ///
    /// TRADING_STATUS_UNKNOWN = 0
    /// TRADING_STATUS_PRE_OPEN = 1
    /// TRADING_STATUS_OPEN = 2
    /// TRADING_STATUS_CLOSE = 3
    /// TRADING_STATUS_TRADING_HALT = 4
    fn status(&self) -> TradingStatusEnum {
        if self.is_out_of_bounds(9) {
            TradingStatusEnum::TradingStatusUnknown.to_le()
        } else {
            TradingStatusEnum::from_le(self.status)
        }
    }

    /// This is the same SymbolID sent by the Client in the MarketDataRequestVLS
    /// message which corresponds to the Symbol that the status in this message
    /// is for.
    fn set_symbol_id(&mut self, value: u32) -> &mut Self {
        if !self.is_out_of_bounds(8) {
            self.symbol_id = value.to_le();
        }
        self
    }

    /// The current trading status for the symbol. Can be one of the following.
    /// The current trading status for the symbol. Can be one of the following.
    ///
    /// TRADING_STATUS_UNKNOWN = 0
    /// TRADING_STATUS_PRE_OPEN = 1
    /// TRADING_STATUS_OPEN = 2
    /// TRADING_STATUS_CLOSE = 3
    /// TRADING_STATUS_TRADING_HALT = 4
    fn set_status(&mut self, value: TradingStatusEnum) -> &mut Self {
        if !self.is_out_of_bounds(9) {
            self.status = unsafe { core::mem::transmute((value as i8).to_le()) };
        }
        self
    }

    #[inline]
    fn clone_safe(&self) -> Self::Safe {
        let mut s = Self::Safe::new();
        self.copy_to(&mut s);
        s
    }

    #[inline]
    fn to_safe(self) -> Self::Safe {
        let mut s = Self::Safe::new();
        self.copy_to(&mut s);
        s
    }
}

#[cfg(test)]
pub(crate) mod tests {
    use super::*;

    #[test]
    pub(crate) fn layout() {
        unsafe {
            assert_eq!(
                12usize,
                core::mem::size_of::<TradingSymbolStatusFixedData>(),
                "TradingSymbolStatusFixedData sizeof expected {:} but was {:}",
                12usize,
                core::mem::size_of::<TradingSymbolStatusFixedData>()
            );
            assert_eq!(
                12u16,
                TradingSymbolStatusFixed::new().size(),
                "TradingSymbolStatusFixed sizeof expected {:} but was {:}",
                12u16,
                TradingSymbolStatusFixed::new().size(),
            );
            assert_eq!(
                TRADING_SYMBOL_STATUS,
                TradingSymbolStatusFixed::new().r#type(),
                "TradingSymbolStatusFixed type expected {:} but was {:}",
                TRADING_SYMBOL_STATUS,
                TradingSymbolStatusFixed::new().r#type(),
            );
            assert_eq!(
                138u16,
                TradingSymbolStatusFixed::new().r#type(),
                "TradingSymbolStatusFixed type expected {:} but was {:}",
                138u16,
                TradingSymbolStatusFixed::new().r#type(),
            );
            let d = TradingSymbolStatusFixedData::new();
            let p = (&d as *const _ as *const u8).offset(0) as usize;
            assert_eq!(
                0usize,
                (core::ptr::addr_of!(d.size) as usize) - p,
                "size offset expected {:} but was {:}",
                0usize,
                (core::ptr::addr_of!(d.size) as usize) - p,
            );
            assert_eq!(
                2usize,
                (core::ptr::addr_of!(d.r#type) as usize) - p,
                "type offset expected {:} but was {:}",
                2usize,
                (core::ptr::addr_of!(d.r#type) as usize) - p,
            );
            assert_eq!(
                4usize,
                (core::ptr::addr_of!(d.symbol_id) as usize) - p,
                "symbol_id offset expected {:} but was {:}",
                4usize,
                (core::ptr::addr_of!(d.symbol_id) as usize) - p,
            );
            assert_eq!(
                8usize,
                (core::ptr::addr_of!(d.status) as usize) - p,
                "status offset expected {:} but was {:}",
                8usize,
                (core::ptr::addr_of!(d.status) as usize) - p,
            );
        }
    }
}
