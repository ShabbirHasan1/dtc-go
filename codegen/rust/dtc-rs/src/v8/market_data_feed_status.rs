// generated by github.com/moontrade/dtc-go/codegen/go at 2022-05-27 08:32:53.747629 +0800 WITA m=+0.008238335
use super::*;

pub(crate) const MARKET_DATA_FEED_STATUS_FIXED_SIZE: usize = 8;

/// size    u16                       = MarketDataFeedStatusFixedSize  (8)
/// type    u16                       = MARKET_DATA_FEED_STATUS  (100)
/// status  MarketDataFeedStatusEnum  = MARKET_DATA_FEED_STATUS_UNSET  (0)
pub(crate) const MARKET_DATA_FEED_STATUS_FIXED_DEFAULT: [u8; 8] = [8, 0, 100, 0, 0, 0, 0, 0];

/// The s_MarketDataFeed_STATUS message is an optional message sent by the
/// Server to indicate the overall status of the market data feed. This status
/// applies to all symbols that have been subscribed to for market data.
pub trait MarketDataFeedStatus: Message {
    type Safe: MarketDataFeedStatus;
    type Unsafe: MarketDataFeedStatus;

    /// This can be set to s_MarketDataFeed_UNAVAILABLE, to indicate the market
    /// data feed is presently not available. Or it can be set to s_MarketDataFeed_AVAILABLE,
    /// to indicate the market data feed has been restored.
    ///
    /// Upon a connection to the server, s_MarketDataFeed_AVAILABLE is assumed
    /// to be the status. It is not until there has been expressly given s_MarketDataFeed_UNAVAILABLE,
    /// will the data feed be considered lost.
    fn status(&self) -> MarketDataFeedStatusEnum;

    /// This can be set to s_MarketDataFeed_UNAVAILABLE, to indicate the market
    /// data feed is presently not available. Or it can be set to s_MarketDataFeed_AVAILABLE,
    /// to indicate the market data feed has been restored.
    ///
    /// Upon a connection to the server, s_MarketDataFeed_AVAILABLE is assumed
    /// to be the status. It is not until there has been expressly given s_MarketDataFeed_UNAVAILABLE,
    /// will the data feed be considered lost.
    fn set_status(&mut self, value: MarketDataFeedStatusEnum) -> &mut Self;

    fn clone_safe(&self) -> Self::Safe;

    fn to_safe(self) -> Self::Safe;

    fn copy_to(&self, to: &mut impl MarketDataFeedStatus) {
        to.set_status(self.status());
    }

    #[inline]
    fn parse<F: Fn(Parsed<Self::Safe, Self::Unsafe>) -> Result<(), crate::Error>>(
        data: &[u8],
        f: F,
    ) -> Result<(), crate::Error> {
        if data.len() < 4 {
            return Err(crate::Error::Malformed("need more data"));
        }
        let size = unsafe { u16::from_le(*(data.as_ptr() as *const u16)) };
        let base_size = if Self::BASE_SIZE_OFFSET == 0 {
            size
        } else {
            let base_size = unsafe {
                u16::from_le(*(data.as_ptr().offset(Self::BASE_SIZE_OFFSET) as *const u16))
            };
            if base_size > size {
                return Err(crate::Error::Malformed("base_size is greater than size"));
            }
            base_size
        };
        if (base_size as usize) >= Self::BASE_SIZE {
            let msg = unsafe { Self::Safe::from_raw_parts(data.as_ptr(), size as usize) };
            let r = f(Parsed::Safe(&msg));
            core::mem::forget(msg);
            r
        } else {
            let msg = unsafe { Self::Unsafe::from_raw_parts(data.as_ptr(), size as usize) };
            let r = f(Parsed::Unsafe(&msg));
            core::mem::forget(msg);
            r
        }
    }
}

/// The s_MarketDataFeed_STATUS message is an optional message sent by the
/// Server to indicate the overall status of the market data feed. This status
/// applies to all symbols that have been subscribed to for market data.
pub struct MarketDataFeedStatusFixed {
    data: *const MarketDataFeedStatusFixedData,
}

pub struct MarketDataFeedStatusFixedUnsafe {
    data: *const MarketDataFeedStatusFixedData,
}

#[repr(packed(8), C)]
pub struct MarketDataFeedStatusFixedData {
    size: u16,
    r#type: u16,
    status: MarketDataFeedStatusEnum,
}

impl MarketDataFeedStatusFixedData {
    pub fn new() -> Self {
        Self {
            size: 8u16.to_le(),
            r#type: MARKET_DATA_FEED_STATUS.to_le(),
            status: MarketDataFeedStatusEnum::MarketDataFeedStatusUnset.to_le(),
        }
    }
}

unsafe impl Send for MarketDataFeedStatusFixed {}
unsafe impl Send for MarketDataFeedStatusFixedUnsafe {}
unsafe impl Send for MarketDataFeedStatusFixedData {}

impl Drop for MarketDataFeedStatusFixed {
    #[inline]
    fn drop(&mut self) {
        crate::deallocate(self.data as *mut u8, self.capacity() as usize);
    }
}

impl Drop for MarketDataFeedStatusFixedUnsafe {
    #[inline]
    fn drop(&mut self) {
        crate::deallocate(self.data as *mut u8, self.capacity() as usize);
    }
}

impl Clone for MarketDataFeedStatusFixed {
    #[inline]
    fn clone(&self) -> Self {
        let mut c = Self::new();
        self.copy_to(&mut c);
        c
    }
}

impl Clone for MarketDataFeedStatusFixedUnsafe {
    #[inline]
    fn clone(&self) -> Self {
        let mut c = Self::new();
        self.copy_to(&mut c);
        c
    }
}

impl Into<Vec<u8>> for MarketDataFeedStatusFixed {
    #[inline]
    fn into(self) -> Vec<u8> {
        self.into_vec()
    }
}

impl Into<Vec<u8>> for MarketDataFeedStatusFixedUnsafe {
    #[inline]
    fn into(self) -> Vec<u8> {
        self.into_vec()
    }
}

impl core::ops::Deref for MarketDataFeedStatusFixed {
    type Target = MarketDataFeedStatusFixedData;

    #[inline]
    fn deref(&self) -> &Self::Target {
        unsafe { &*self.data }
    }
}

impl core::ops::DerefMut for MarketDataFeedStatusFixed {
    #[inline]
    fn deref_mut(&mut self) -> &mut Self::Target {
        unsafe { &mut *(self.data as *mut Self::Target) }
    }
}

impl core::ops::Deref for MarketDataFeedStatusFixedUnsafe {
    type Target = MarketDataFeedStatusFixedData;

    #[inline]
    fn deref(&self) -> &Self::Target {
        unsafe { &*self.data }
    }
}

impl core::ops::DerefMut for MarketDataFeedStatusFixedUnsafe {
    #[inline]
    fn deref_mut(&mut self) -> &mut Self::Target {
        unsafe { &mut *(self.data as *mut Self::Target) }
    }
}

impl core::fmt::Display for MarketDataFeedStatusFixed {
    #[inline]
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(
            format!(
                "MarketDataFeedStatusFixed(size: {}, type: {}, status: {})",
                self.size(),
                self.r#type(),
                self.status()
            )
            .as_str(),
        )
    }
}

impl core::fmt::Debug for MarketDataFeedStatusFixed {
    #[inline]
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(
            format!(
                "MarketDataFeedStatusFixed(size: {}, type: {}, status: {})",
                self.size(),
                self.r#type(),
                self.status()
            )
            .as_str(),
        )
    }
}

impl core::fmt::Display for MarketDataFeedStatusFixedUnsafe {
    #[inline]
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(
            format!(
                "MarketDataFeedStatusFixedUnsafe(size: {}, type: {}, status: {})",
                self.size(),
                self.r#type(),
                self.status()
            )
            .as_str(),
        )
    }
}

impl core::fmt::Debug for MarketDataFeedStatusFixedUnsafe {
    #[inline]
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(
            format!(
                "MarketDataFeedStatusFixedUnsafe(size: {}, type: {}, status: {})",
                self.size(),
                self.r#type(),
                self.status()
            )
            .as_str(),
        )
    }
}

impl crate::Message for MarketDataFeedStatusFixed {
    type Data = MarketDataFeedStatusFixedData;

    const TYPE: u16 = MARKET_DATA_FEED_STATUS;
    const BASE_SIZE: usize = 8;
    const BASE_SIZE_OFFSET: isize = 0;

    #[inline]
    fn new() -> Self {
        Self {
            data: crate::allocate(Self::BASE_SIZE, MarketDataFeedStatusFixedData::new()),
        }
    }

    #[inline]
    fn size(&self) -> u16 {
        u16::from_le(self.size)
    }

    #[inline]
    fn r#type(&self) -> u16 {
        u16::from_le(self.r#type)
    }

    #[inline]
    fn base_size(&self) -> u16 {
        u16::from_le(self.size)
    }

    #[inline]
    fn capacity(&self) -> u16 {
        u16::from_le(self.size)
    }

    #[inline]
    unsafe fn as_ptr(&self) -> *const Self::Data {
        self.data
    }

    #[inline]
    unsafe fn from_raw_parts(data: *const u8, _: usize) -> Self {
        Self {
            data: data as *const MarketDataFeedStatusFixedData,
        }
    }
}
impl crate::Message for MarketDataFeedStatusFixedUnsafe {
    type Data = MarketDataFeedStatusFixedData;

    const TYPE: u16 = MARKET_DATA_FEED_STATUS;
    const BASE_SIZE: usize = 8;
    const BASE_SIZE_OFFSET: isize = 0;

    #[inline]
    fn new() -> Self {
        Self {
            data: crate::allocate(Self::BASE_SIZE, MarketDataFeedStatusFixedData::new()),
        }
    }

    #[inline]
    fn size(&self) -> u16 {
        u16::from_le(self.size)
    }

    #[inline]
    fn r#type(&self) -> u16 {
        u16::from_le(self.r#type)
    }

    #[inline]
    fn base_size(&self) -> u16 {
        u16::from_le(self.size)
    }

    #[inline]
    fn capacity(&self) -> u16 {
        u16::from_le(self.size)
    }

    #[inline]
    unsafe fn as_ptr(&self) -> *const Self::Data {
        self.data
    }

    #[inline]
    unsafe fn from_raw_parts(data: *const u8, _: usize) -> Self {
        Self {
            data: data as *const MarketDataFeedStatusFixedData,
        }
    }
}
/// The s_MarketDataFeed_STATUS message is an optional message sent by the
/// Server to indicate the overall status of the market data feed. This status
/// applies to all symbols that have been subscribed to for market data.
impl MarketDataFeedStatus for MarketDataFeedStatusFixed {
    type Safe = MarketDataFeedStatusFixed;
    type Unsafe = MarketDataFeedStatusFixedUnsafe;

    /// This can be set to s_MarketDataFeed_UNAVAILABLE, to indicate the market
    /// data feed is presently not available. Or it can be set to s_MarketDataFeed_AVAILABLE,
    /// to indicate the market data feed has been restored.
    ///
    /// Upon a connection to the server, s_MarketDataFeed_AVAILABLE is assumed
    /// to be the status. It is not until there has been expressly given s_MarketDataFeed_UNAVAILABLE,
    /// will the data feed be considered lost.
    fn status(&self) -> MarketDataFeedStatusEnum {
        MarketDataFeedStatusEnum::from_le(self.status)
    }

    /// This can be set to s_MarketDataFeed_UNAVAILABLE, to indicate the market
    /// data feed is presently not available. Or it can be set to s_MarketDataFeed_AVAILABLE,
    /// to indicate the market data feed has been restored.
    ///
    /// Upon a connection to the server, s_MarketDataFeed_AVAILABLE is assumed
    /// to be the status. It is not until there has been expressly given s_MarketDataFeed_UNAVAILABLE,
    /// will the data feed be considered lost.
    fn set_status(&mut self, value: MarketDataFeedStatusEnum) -> &mut Self {
        self.status = unsafe { core::mem::transmute((value as i32).to_le()) };
        self
    }

    #[inline]
    fn clone_safe(&self) -> Self::Safe {
        let mut s = Self::Safe::new();
        self.copy_to(&mut s);
        s
    }

    #[inline]
    fn to_safe(self) -> Self::Safe {
        self
    }
}

/// The s_MarketDataFeed_STATUS message is an optional message sent by the
/// Server to indicate the overall status of the market data feed. This status
/// applies to all symbols that have been subscribed to for market data.
impl MarketDataFeedStatus for MarketDataFeedStatusFixedUnsafe {
    type Safe = MarketDataFeedStatusFixed;
    type Unsafe = MarketDataFeedStatusFixedUnsafe;

    /// This can be set to s_MarketDataFeed_UNAVAILABLE, to indicate the market
    /// data feed is presently not available. Or it can be set to s_MarketDataFeed_AVAILABLE,
    /// to indicate the market data feed has been restored.
    ///
    /// Upon a connection to the server, s_MarketDataFeed_AVAILABLE is assumed
    /// to be the status. It is not until there has been expressly given s_MarketDataFeed_UNAVAILABLE,
    /// will the data feed be considered lost.
    fn status(&self) -> MarketDataFeedStatusEnum {
        if self.is_out_of_bounds(8) {
            MarketDataFeedStatusEnum::MarketDataFeedStatusUnset.to_le()
        } else {
            MarketDataFeedStatusEnum::from_le(self.status)
        }
    }

    /// This can be set to s_MarketDataFeed_UNAVAILABLE, to indicate the market
    /// data feed is presently not available. Or it can be set to s_MarketDataFeed_AVAILABLE,
    /// to indicate the market data feed has been restored.
    ///
    /// Upon a connection to the server, s_MarketDataFeed_AVAILABLE is assumed
    /// to be the status. It is not until there has been expressly given s_MarketDataFeed_UNAVAILABLE,
    /// will the data feed be considered lost.
    fn set_status(&mut self, value: MarketDataFeedStatusEnum) -> &mut Self {
        if !self.is_out_of_bounds(8) {
            self.status = unsafe { core::mem::transmute((value as i32).to_le()) };
        }
        self
    }

    #[inline]
    fn clone_safe(&self) -> Self::Safe {
        let mut s = Self::Safe::new();
        self.copy_to(&mut s);
        s
    }

    #[inline]
    fn to_safe(self) -> Self::Safe {
        let mut s = Self::Safe::new();
        self.copy_to(&mut s);
        s
    }
}

#[cfg(test)]
pub(crate) mod tests {
    use super::*;

    #[test]
    pub(crate) fn layout() {
        unsafe {
            assert_eq!(
                8usize,
                core::mem::size_of::<MarketDataFeedStatusFixedData>(),
                "MarketDataFeedStatusFixedData sizeof expected {:} but was {:}",
                8usize,
                core::mem::size_of::<MarketDataFeedStatusFixedData>()
            );
            assert_eq!(
                8u16,
                MarketDataFeedStatusFixed::new().size(),
                "MarketDataFeedStatusFixed sizeof expected {:} but was {:}",
                8u16,
                MarketDataFeedStatusFixed::new().size(),
            );
            assert_eq!(
                MARKET_DATA_FEED_STATUS,
                MarketDataFeedStatusFixed::new().r#type(),
                "MarketDataFeedStatusFixed type expected {:} but was {:}",
                MARKET_DATA_FEED_STATUS,
                MarketDataFeedStatusFixed::new().r#type(),
            );
            assert_eq!(
                100u16,
                MarketDataFeedStatusFixed::new().r#type(),
                "MarketDataFeedStatusFixed type expected {:} but was {:}",
                100u16,
                MarketDataFeedStatusFixed::new().r#type(),
            );
            let d = MarketDataFeedStatusFixedData::new();
            let p = (&d as *const _ as *const u8).offset(0) as usize;
            assert_eq!(
                0usize,
                (core::ptr::addr_of!(d.size) as usize) - p,
                "size offset expected {:} but was {:}",
                0usize,
                (core::ptr::addr_of!(d.size) as usize) - p,
            );
            assert_eq!(
                2usize,
                (core::ptr::addr_of!(d.r#type) as usize) - p,
                "type offset expected {:} but was {:}",
                2usize,
                (core::ptr::addr_of!(d.r#type) as usize) - p,
            );
            assert_eq!(
                4usize,
                (core::ptr::addr_of!(d.status) as usize) - p,
                "status offset expected {:} but was {:}",
                4usize,
                (core::ptr::addr_of!(d.status) as usize) - p,
            );
        }
    }
}
