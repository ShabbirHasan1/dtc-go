// generated by github.com/moontrade/dtc-go/codegen/go at 2022-05-27 08:32:53.747629 +0800 WITA m=+0.008238335
use super::*;

pub(crate) const MARKET_DATA_UPDATE_TRADING_SESSION_DATE_FIXED_SIZE: usize = 12;

/// size       u16            = MarketDataUpdateTradingSessionDateFixedSize  (12)
/// type       u16            = MARKET_DATA_UPDATE_TRADING_SESSION_DATE  (136)
/// symbol_id  u32            = 0
/// date       DateTime4Byte  = 0
pub(crate) const MARKET_DATA_UPDATE_TRADING_SESSION_DATE_FIXED_DEFAULT: [u8; 12] =
    [12, 0, 136, 0, 0, 0, 0, 0, 0, 0, 0, 0];

/// Sent by the Server to the Client to update the trading session Date.
pub trait MarketDataUpdateTradingSessionDate: Message {
    type Safe: MarketDataUpdateTradingSessionDate;
    type Unsafe: MarketDataUpdateTradingSessionDate;

    /// This is the same SymbolID sent by the Client in the MarketDataRequestVLS
    /// message which corresponds to the Symbol that the data in this message
    /// is for.
    fn symbol_id(&self) -> u32;

    /// The date of the current trading session. The time component is not normally
    /// considered relevant in this case.
    fn date(&self) -> DateTime4Byte;

    /// This is the same SymbolID sent by the Client in the MarketDataRequestVLS
    /// message which corresponds to the Symbol that the data in this message
    /// is for.
    fn set_symbol_id(&mut self, value: u32) -> &mut Self;

    /// The date of the current trading session. The time component is not normally
    /// considered relevant in this case.
    fn set_date(&mut self, value: DateTime4Byte) -> &mut Self;

    fn clone_safe(&self) -> Self::Safe;

    fn to_safe(self) -> Self::Safe;

    fn copy_to(&self, to: &mut impl MarketDataUpdateTradingSessionDate) {
        to.set_symbol_id(self.symbol_id());
        to.set_date(self.date());
    }

    #[inline]
    fn parse<F: Fn(Parsed<Self::Safe, Self::Unsafe>) -> Result<(), crate::Error>>(
        data: &[u8],
        f: F,
    ) -> Result<(), crate::Error> {
        if data.len() < 4 {
            return Err(crate::Error::Malformed("need more data"));
        }
        let size = unsafe { u16::from_le(*(data.as_ptr() as *const u16)) };
        let base_size = if Self::BASE_SIZE_OFFSET == 0 {
            size
        } else {
            let base_size = unsafe {
                u16::from_le(*(data.as_ptr().offset(Self::BASE_SIZE_OFFSET) as *const u16))
            };
            if base_size > size {
                return Err(crate::Error::Malformed("base_size is greater than size"));
            }
            base_size
        };
        if (base_size as usize) >= Self::BASE_SIZE {
            let msg = unsafe { Self::Safe::from_raw_parts(data.as_ptr(), size as usize) };
            let r = f(Parsed::Safe(&msg));
            core::mem::forget(msg);
            r
        } else {
            let msg = unsafe { Self::Unsafe::from_raw_parts(data.as_ptr(), size as usize) };
            let r = f(Parsed::Unsafe(&msg));
            core::mem::forget(msg);
            r
        }
    }
}

/// Sent by the Server to the Client to update the trading session Date.
pub struct MarketDataUpdateTradingSessionDateFixed {
    data: *const MarketDataUpdateTradingSessionDateFixedData,
}

pub struct MarketDataUpdateTradingSessionDateFixedUnsafe {
    data: *const MarketDataUpdateTradingSessionDateFixedData,
}

#[repr(packed(8), C)]
pub struct MarketDataUpdateTradingSessionDateFixedData {
    size: u16,
    r#type: u16,
    symbol_id: u32,
    date: DateTime4Byte,
}

impl MarketDataUpdateTradingSessionDateFixedData {
    pub fn new() -> Self {
        Self {
            size: 12u16.to_le(),
            r#type: MARKET_DATA_UPDATE_TRADING_SESSION_DATE.to_le(),
            symbol_id: 0u32,
            date: 0u32,
        }
    }
}

unsafe impl Send for MarketDataUpdateTradingSessionDateFixed {}
unsafe impl Send for MarketDataUpdateTradingSessionDateFixedUnsafe {}
unsafe impl Send for MarketDataUpdateTradingSessionDateFixedData {}

impl Drop for MarketDataUpdateTradingSessionDateFixed {
    #[inline]
    fn drop(&mut self) {
        crate::deallocate(self.data as *mut u8, self.capacity() as usize);
    }
}

impl Drop for MarketDataUpdateTradingSessionDateFixedUnsafe {
    #[inline]
    fn drop(&mut self) {
        crate::deallocate(self.data as *mut u8, self.capacity() as usize);
    }
}

impl Clone for MarketDataUpdateTradingSessionDateFixed {
    #[inline]
    fn clone(&self) -> Self {
        let mut c = Self::new();
        self.copy_to(&mut c);
        c
    }
}

impl Clone for MarketDataUpdateTradingSessionDateFixedUnsafe {
    #[inline]
    fn clone(&self) -> Self {
        let mut c = Self::new();
        self.copy_to(&mut c);
        c
    }
}

impl Into<Vec<u8>> for MarketDataUpdateTradingSessionDateFixed {
    #[inline]
    fn into(self) -> Vec<u8> {
        self.into_vec()
    }
}

impl Into<Vec<u8>> for MarketDataUpdateTradingSessionDateFixedUnsafe {
    #[inline]
    fn into(self) -> Vec<u8> {
        self.into_vec()
    }
}

impl core::ops::Deref for MarketDataUpdateTradingSessionDateFixed {
    type Target = MarketDataUpdateTradingSessionDateFixedData;

    #[inline]
    fn deref(&self) -> &Self::Target {
        unsafe { &*self.data }
    }
}

impl core::ops::DerefMut for MarketDataUpdateTradingSessionDateFixed {
    #[inline]
    fn deref_mut(&mut self) -> &mut Self::Target {
        unsafe { &mut *(self.data as *mut Self::Target) }
    }
}

impl core::ops::Deref for MarketDataUpdateTradingSessionDateFixedUnsafe {
    type Target = MarketDataUpdateTradingSessionDateFixedData;

    #[inline]
    fn deref(&self) -> &Self::Target {
        unsafe { &*self.data }
    }
}

impl core::ops::DerefMut for MarketDataUpdateTradingSessionDateFixedUnsafe {
    #[inline]
    fn deref_mut(&mut self) -> &mut Self::Target {
        unsafe { &mut *(self.data as *mut Self::Target) }
    }
}

impl core::fmt::Display for MarketDataUpdateTradingSessionDateFixed {
    #[inline]
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(format!("MarketDataUpdateTradingSessionDateFixed(size: {}, type: {}, symbol_id: {}, date: {})", self.size(), self.r#type(), self.symbol_id(), self.date()).as_str())
    }
}

impl core::fmt::Debug for MarketDataUpdateTradingSessionDateFixed {
    #[inline]
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(format!("MarketDataUpdateTradingSessionDateFixed(size: {}, type: {}, symbol_id: {}, date: {})", self.size(), self.r#type(), self.symbol_id(), self.date()).as_str())
    }
}

impl core::fmt::Display for MarketDataUpdateTradingSessionDateFixedUnsafe {
    #[inline]
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(format!("MarketDataUpdateTradingSessionDateFixedUnsafe(size: {}, type: {}, symbol_id: {}, date: {})", self.size(), self.r#type(), self.symbol_id(), self.date()).as_str())
    }
}

impl core::fmt::Debug for MarketDataUpdateTradingSessionDateFixedUnsafe {
    #[inline]
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(format!("MarketDataUpdateTradingSessionDateFixedUnsafe(size: {}, type: {}, symbol_id: {}, date: {})", self.size(), self.r#type(), self.symbol_id(), self.date()).as_str())
    }
}

impl crate::Message for MarketDataUpdateTradingSessionDateFixed {
    type Data = MarketDataUpdateTradingSessionDateFixedData;

    const TYPE: u16 = MARKET_DATA_UPDATE_TRADING_SESSION_DATE;
    const BASE_SIZE: usize = 12;
    const BASE_SIZE_OFFSET: isize = 0;

    #[inline]
    fn new() -> Self {
        Self {
            data: crate::allocate(
                Self::BASE_SIZE,
                MarketDataUpdateTradingSessionDateFixedData::new(),
            ),
        }
    }

    #[inline]
    fn size(&self) -> u16 {
        u16::from_le(self.size)
    }

    #[inline]
    fn r#type(&self) -> u16 {
        u16::from_le(self.r#type)
    }

    #[inline]
    fn base_size(&self) -> u16 {
        u16::from_le(self.size)
    }

    #[inline]
    fn capacity(&self) -> u16 {
        u16::from_le(self.size)
    }

    #[inline]
    unsafe fn as_ptr(&self) -> *const Self::Data {
        self.data
    }

    #[inline]
    unsafe fn from_raw_parts(data: *const u8, _: usize) -> Self {
        Self {
            data: data as *const MarketDataUpdateTradingSessionDateFixedData,
        }
    }
}
impl crate::Message for MarketDataUpdateTradingSessionDateFixedUnsafe {
    type Data = MarketDataUpdateTradingSessionDateFixedData;

    const TYPE: u16 = MARKET_DATA_UPDATE_TRADING_SESSION_DATE;
    const BASE_SIZE: usize = 12;
    const BASE_SIZE_OFFSET: isize = 0;

    #[inline]
    fn new() -> Self {
        Self {
            data: crate::allocate(
                Self::BASE_SIZE,
                MarketDataUpdateTradingSessionDateFixedData::new(),
            ),
        }
    }

    #[inline]
    fn size(&self) -> u16 {
        u16::from_le(self.size)
    }

    #[inline]
    fn r#type(&self) -> u16 {
        u16::from_le(self.r#type)
    }

    #[inline]
    fn base_size(&self) -> u16 {
        u16::from_le(self.size)
    }

    #[inline]
    fn capacity(&self) -> u16 {
        u16::from_le(self.size)
    }

    #[inline]
    unsafe fn as_ptr(&self) -> *const Self::Data {
        self.data
    }

    #[inline]
    unsafe fn from_raw_parts(data: *const u8, _: usize) -> Self {
        Self {
            data: data as *const MarketDataUpdateTradingSessionDateFixedData,
        }
    }
}
/// Sent by the Server to the Client to update the trading session Date.
impl MarketDataUpdateTradingSessionDate for MarketDataUpdateTradingSessionDateFixed {
    type Safe = MarketDataUpdateTradingSessionDateFixed;
    type Unsafe = MarketDataUpdateTradingSessionDateFixedUnsafe;

    /// This is the same SymbolID sent by the Client in the MarketDataRequestVLS
    /// message which corresponds to the Symbol that the data in this message
    /// is for.
    fn symbol_id(&self) -> u32 {
        u32::from_le(self.symbol_id)
    }

    /// The date of the current trading session. The time component is not normally
    /// considered relevant in this case.
    fn date(&self) -> DateTime4Byte {
        u32::from_le(self.date)
    }

    /// This is the same SymbolID sent by the Client in the MarketDataRequestVLS
    /// message which corresponds to the Symbol that the data in this message
    /// is for.
    fn set_symbol_id(&mut self, value: u32) -> &mut Self {
        self.symbol_id = value.to_le();
        self
    }

    /// The date of the current trading session. The time component is not normally
    /// considered relevant in this case.
    fn set_date(&mut self, value: DateTime4Byte) -> &mut Self {
        self.date = value.to_le();
        self
    }

    #[inline]
    fn clone_safe(&self) -> Self::Safe {
        let mut s = Self::Safe::new();
        self.copy_to(&mut s);
        s
    }

    #[inline]
    fn to_safe(self) -> Self::Safe {
        self
    }
}

/// Sent by the Server to the Client to update the trading session Date.
impl MarketDataUpdateTradingSessionDate for MarketDataUpdateTradingSessionDateFixedUnsafe {
    type Safe = MarketDataUpdateTradingSessionDateFixed;
    type Unsafe = MarketDataUpdateTradingSessionDateFixedUnsafe;

    /// This is the same SymbolID sent by the Client in the MarketDataRequestVLS
    /// message which corresponds to the Symbol that the data in this message
    /// is for.
    fn symbol_id(&self) -> u32 {
        if self.is_out_of_bounds(8) {
            0u32
        } else {
            u32::from_le(self.symbol_id)
        }
    }

    /// The date of the current trading session. The time component is not normally
    /// considered relevant in this case.
    fn date(&self) -> DateTime4Byte {
        if self.is_out_of_bounds(12) {
            0u32
        } else {
            u32::from_le(self.date)
        }
    }

    /// This is the same SymbolID sent by the Client in the MarketDataRequestVLS
    /// message which corresponds to the Symbol that the data in this message
    /// is for.
    fn set_symbol_id(&mut self, value: u32) -> &mut Self {
        if !self.is_out_of_bounds(8) {
            self.symbol_id = value.to_le();
        }
        self
    }

    /// The date of the current trading session. The time component is not normally
    /// considered relevant in this case.
    fn set_date(&mut self, value: DateTime4Byte) -> &mut Self {
        if !self.is_out_of_bounds(12) {
            self.date = value.to_le();
        }
        self
    }

    #[inline]
    fn clone_safe(&self) -> Self::Safe {
        let mut s = Self::Safe::new();
        self.copy_to(&mut s);
        s
    }

    #[inline]
    fn to_safe(self) -> Self::Safe {
        let mut s = Self::Safe::new();
        self.copy_to(&mut s);
        s
    }
}

#[cfg(test)]
pub(crate) mod tests {
    use super::*;

    #[test]
    pub(crate) fn layout() {
        unsafe {
            assert_eq!(
                12usize,
                core::mem::size_of::<MarketDataUpdateTradingSessionDateFixedData>(),
                "MarketDataUpdateTradingSessionDateFixedData sizeof expected {:} but was {:}",
                12usize,
                core::mem::size_of::<MarketDataUpdateTradingSessionDateFixedData>()
            );
            assert_eq!(
                12u16,
                MarketDataUpdateTradingSessionDateFixed::new().size(),
                "MarketDataUpdateTradingSessionDateFixed sizeof expected {:} but was {:}",
                12u16,
                MarketDataUpdateTradingSessionDateFixed::new().size(),
            );
            assert_eq!(
                MARKET_DATA_UPDATE_TRADING_SESSION_DATE,
                MarketDataUpdateTradingSessionDateFixed::new().r#type(),
                "MarketDataUpdateTradingSessionDateFixed type expected {:} but was {:}",
                MARKET_DATA_UPDATE_TRADING_SESSION_DATE,
                MarketDataUpdateTradingSessionDateFixed::new().r#type(),
            );
            assert_eq!(
                136u16,
                MarketDataUpdateTradingSessionDateFixed::new().r#type(),
                "MarketDataUpdateTradingSessionDateFixed type expected {:} but was {:}",
                136u16,
                MarketDataUpdateTradingSessionDateFixed::new().r#type(),
            );
            let d = MarketDataUpdateTradingSessionDateFixedData::new();
            let p = (&d as *const _ as *const u8).offset(0) as usize;
            assert_eq!(
                0usize,
                (core::ptr::addr_of!(d.size) as usize) - p,
                "size offset expected {:} but was {:}",
                0usize,
                (core::ptr::addr_of!(d.size) as usize) - p,
            );
            assert_eq!(
                2usize,
                (core::ptr::addr_of!(d.r#type) as usize) - p,
                "type offset expected {:} but was {:}",
                2usize,
                (core::ptr::addr_of!(d.r#type) as usize) - p,
            );
            assert_eq!(
                4usize,
                (core::ptr::addr_of!(d.symbol_id) as usize) - p,
                "symbol_id offset expected {:} but was {:}",
                4usize,
                (core::ptr::addr_of!(d.symbol_id) as usize) - p,
            );
            assert_eq!(
                8usize,
                (core::ptr::addr_of!(d.date) as usize) - p,
                "date offset expected {:} but was {:}",
                8usize,
                (core::ptr::addr_of!(d.date) as usize) - p,
            );
        }
    }
}
