// generated by github.com/moontrade/dtc-go/codegen/go at 2022-05-27 08:32:53.747629 +0800 WITA m=+0.008238335
use super::*;

pub(crate) const MARKET_ORDERS_SNAPSHOT_MESSAGE_BOUNDARY_FIXED_SIZE: usize = 9;

/// size              u16                     = MarketOrdersSnapshotMessageBoundaryFixedSize  (9)
/// type              u16                     = MARKET_ORDERS_SNAPSHOT_MESSAGE_BOUNDARY  (155)
/// symbol_id         u32                     = 0
/// message_boundary  MessageSetBoundaryEnum  = MESSAGE_SET_BOUNDARY_UNSET  (0)
pub(crate) const MARKET_ORDERS_SNAPSHOT_MESSAGE_BOUNDARY_FIXED_DEFAULT: [u8; 9] =
    [9, 0, 155, 0, 0, 0, 0, 0, 0];

pub trait MarketOrdersSnapshotMessageBoundary: Message {
    type Safe: MarketOrdersSnapshotMessageBoundary;
    type Unsafe: MarketOrdersSnapshotMessageBoundary;

    fn symbol_id(&self) -> u32;

    fn message_boundary(&self) -> MessageSetBoundaryEnum;

    fn set_symbol_id(&mut self, value: u32) -> &mut Self;

    fn set_message_boundary(&mut self, value: MessageSetBoundaryEnum) -> &mut Self;

    fn clone_safe(&self) -> Self::Safe;

    fn to_safe(self) -> Self::Safe;

    fn copy_to(&self, to: &mut impl MarketOrdersSnapshotMessageBoundary) {
        to.set_symbol_id(self.symbol_id());
        to.set_message_boundary(self.message_boundary());
    }

    #[inline]
    fn parse<F: Fn(Parsed<Self::Safe, Self::Unsafe>) -> Result<(), crate::Error>>(
        data: &[u8],
        f: F,
    ) -> Result<(), crate::Error> {
        if data.len() < 4 {
            return Err(crate::Error::Malformed("need more data"));
        }
        let size = unsafe { u16::from_le(*(data.as_ptr() as *const u16)) };
        let base_size = if Self::BASE_SIZE_OFFSET == 0 {
            size
        } else {
            let base_size = unsafe {
                u16::from_le(*(data.as_ptr().offset(Self::BASE_SIZE_OFFSET) as *const u16))
            };
            if base_size > size {
                return Err(crate::Error::Malformed("base_size is greater than size"));
            }
            base_size
        };
        if (base_size as usize) >= Self::BASE_SIZE {
            let msg = unsafe { Self::Safe::from_raw_parts(data.as_ptr(), size as usize) };
            let r = f(Parsed::Safe(&msg));
            core::mem::forget(msg);
            r
        } else {
            let msg = unsafe { Self::Unsafe::from_raw_parts(data.as_ptr(), size as usize) };
            let r = f(Parsed::Unsafe(&msg));
            core::mem::forget(msg);
            r
        }
    }
}

pub struct MarketOrdersSnapshotMessageBoundaryFixed {
    data: *const MarketOrdersSnapshotMessageBoundaryFixedData,
}

pub struct MarketOrdersSnapshotMessageBoundaryFixedUnsafe {
    data: *const MarketOrdersSnapshotMessageBoundaryFixedData,
}

#[repr(packed(1), C)]
pub struct MarketOrdersSnapshotMessageBoundaryFixedData {
    size: u16,
    r#type: u16,
    symbol_id: u32,
    message_boundary: MessageSetBoundaryEnum,
}

impl MarketOrdersSnapshotMessageBoundaryFixedData {
    pub fn new() -> Self {
        Self {
            size: 9u16.to_le(),
            r#type: MARKET_ORDERS_SNAPSHOT_MESSAGE_BOUNDARY.to_le(),
            symbol_id: 0u32.to_le(),
            message_boundary: MessageSetBoundaryEnum::MessageSetBoundaryUnset.to_le(),
        }
    }
}

unsafe impl Send for MarketOrdersSnapshotMessageBoundaryFixed {}
unsafe impl Send for MarketOrdersSnapshotMessageBoundaryFixedUnsafe {}
unsafe impl Send for MarketOrdersSnapshotMessageBoundaryFixedData {}

impl Drop for MarketOrdersSnapshotMessageBoundaryFixed {
    #[inline]
    fn drop(&mut self) {
        crate::deallocate(self.data as *mut u8, self.capacity() as usize);
    }
}

impl Drop for MarketOrdersSnapshotMessageBoundaryFixedUnsafe {
    #[inline]
    fn drop(&mut self) {
        crate::deallocate(self.data as *mut u8, self.capacity() as usize);
    }
}

impl Clone for MarketOrdersSnapshotMessageBoundaryFixed {
    #[inline]
    fn clone(&self) -> Self {
        let mut c = Self::new();
        self.copy_to(&mut c);
        c
    }
}

impl Clone for MarketOrdersSnapshotMessageBoundaryFixedUnsafe {
    #[inline]
    fn clone(&self) -> Self {
        let mut c = Self::new();
        self.copy_to(&mut c);
        c
    }
}

impl Into<Vec<u8>> for MarketOrdersSnapshotMessageBoundaryFixed {
    #[inline]
    fn into(self) -> Vec<u8> {
        self.into_vec()
    }
}

impl Into<Vec<u8>> for MarketOrdersSnapshotMessageBoundaryFixedUnsafe {
    #[inline]
    fn into(self) -> Vec<u8> {
        self.into_vec()
    }
}

impl core::ops::Deref for MarketOrdersSnapshotMessageBoundaryFixed {
    type Target = MarketOrdersSnapshotMessageBoundaryFixedData;

    #[inline]
    fn deref(&self) -> &Self::Target {
        unsafe { &*self.data }
    }
}

impl core::ops::DerefMut for MarketOrdersSnapshotMessageBoundaryFixed {
    #[inline]
    fn deref_mut(&mut self) -> &mut Self::Target {
        unsafe { &mut *(self.data as *mut Self::Target) }
    }
}

impl core::ops::Deref for MarketOrdersSnapshotMessageBoundaryFixedUnsafe {
    type Target = MarketOrdersSnapshotMessageBoundaryFixedData;

    #[inline]
    fn deref(&self) -> &Self::Target {
        unsafe { &*self.data }
    }
}

impl core::ops::DerefMut for MarketOrdersSnapshotMessageBoundaryFixedUnsafe {
    #[inline]
    fn deref_mut(&mut self) -> &mut Self::Target {
        unsafe { &mut *(self.data as *mut Self::Target) }
    }
}

impl core::fmt::Display for MarketOrdersSnapshotMessageBoundaryFixed {
    #[inline]
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(format!("MarketOrdersSnapshotMessageBoundaryFixed(size: {}, type: {}, symbol_id: {}, message_boundary: {})", self.size(), self.r#type(), self.symbol_id(), self.message_boundary()).as_str())
    }
}

impl core::fmt::Debug for MarketOrdersSnapshotMessageBoundaryFixed {
    #[inline]
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(format!("MarketOrdersSnapshotMessageBoundaryFixed(size: {}, type: {}, symbol_id: {}, message_boundary: {})", self.size(), self.r#type(), self.symbol_id(), self.message_boundary()).as_str())
    }
}

impl core::fmt::Display for MarketOrdersSnapshotMessageBoundaryFixedUnsafe {
    #[inline]
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(format!("MarketOrdersSnapshotMessageBoundaryFixedUnsafe(size: {}, type: {}, symbol_id: {}, message_boundary: {})", self.size(), self.r#type(), self.symbol_id(), self.message_boundary()).as_str())
    }
}

impl core::fmt::Debug for MarketOrdersSnapshotMessageBoundaryFixedUnsafe {
    #[inline]
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(format!("MarketOrdersSnapshotMessageBoundaryFixedUnsafe(size: {}, type: {}, symbol_id: {}, message_boundary: {})", self.size(), self.r#type(), self.symbol_id(), self.message_boundary()).as_str())
    }
}

impl crate::Message for MarketOrdersSnapshotMessageBoundaryFixed {
    type Data = MarketOrdersSnapshotMessageBoundaryFixedData;

    const TYPE: u16 = MARKET_ORDERS_SNAPSHOT_MESSAGE_BOUNDARY;
    const BASE_SIZE: usize = 9;
    const BASE_SIZE_OFFSET: isize = 0;

    #[inline]
    fn new() -> Self {
        Self {
            data: crate::allocate(
                Self::BASE_SIZE,
                MarketOrdersSnapshotMessageBoundaryFixedData::new(),
            ),
        }
    }

    #[inline]
    fn size(&self) -> u16 {
        u16::from_le(self.size)
    }

    #[inline]
    fn r#type(&self) -> u16 {
        u16::from_le(self.r#type)
    }

    #[inline]
    fn base_size(&self) -> u16 {
        u16::from_le(self.size)
    }

    #[inline]
    fn capacity(&self) -> u16 {
        u16::from_le(self.size)
    }

    #[inline]
    unsafe fn as_ptr(&self) -> *const Self::Data {
        self.data
    }

    #[inline]
    unsafe fn from_raw_parts(data: *const u8, _: usize) -> Self {
        Self {
            data: data as *const MarketOrdersSnapshotMessageBoundaryFixedData,
        }
    }
}
impl crate::Message for MarketOrdersSnapshotMessageBoundaryFixedUnsafe {
    type Data = MarketOrdersSnapshotMessageBoundaryFixedData;

    const TYPE: u16 = MARKET_ORDERS_SNAPSHOT_MESSAGE_BOUNDARY;
    const BASE_SIZE: usize = 9;
    const BASE_SIZE_OFFSET: isize = 0;

    #[inline]
    fn new() -> Self {
        Self {
            data: crate::allocate(
                Self::BASE_SIZE,
                MarketOrdersSnapshotMessageBoundaryFixedData::new(),
            ),
        }
    }

    #[inline]
    fn size(&self) -> u16 {
        u16::from_le(self.size)
    }

    #[inline]
    fn r#type(&self) -> u16 {
        u16::from_le(self.r#type)
    }

    #[inline]
    fn base_size(&self) -> u16 {
        u16::from_le(self.size)
    }

    #[inline]
    fn capacity(&self) -> u16 {
        u16::from_le(self.size)
    }

    #[inline]
    unsafe fn as_ptr(&self) -> *const Self::Data {
        self.data
    }

    #[inline]
    unsafe fn from_raw_parts(data: *const u8, _: usize) -> Self {
        Self {
            data: data as *const MarketOrdersSnapshotMessageBoundaryFixedData,
        }
    }
}
impl MarketOrdersSnapshotMessageBoundary for MarketOrdersSnapshotMessageBoundaryFixed {
    type Safe = MarketOrdersSnapshotMessageBoundaryFixed;
    type Unsafe = MarketOrdersSnapshotMessageBoundaryFixedUnsafe;

    fn symbol_id(&self) -> u32 {
        u32::from_le(self.symbol_id)
    }

    fn message_boundary(&self) -> MessageSetBoundaryEnum {
        MessageSetBoundaryEnum::from_le(self.message_boundary)
    }

    fn set_symbol_id(&mut self, value: u32) -> &mut Self {
        self.symbol_id = value.to_le();
        self
    }

    fn set_message_boundary(&mut self, value: MessageSetBoundaryEnum) -> &mut Self {
        self.message_boundary = unsafe { core::mem::transmute((value as u8).to_le()) };
        self
    }

    #[inline]
    fn clone_safe(&self) -> Self::Safe {
        let mut s = Self::Safe::new();
        self.copy_to(&mut s);
        s
    }

    #[inline]
    fn to_safe(self) -> Self::Safe {
        self
    }
}

impl MarketOrdersSnapshotMessageBoundary for MarketOrdersSnapshotMessageBoundaryFixedUnsafe {
    type Safe = MarketOrdersSnapshotMessageBoundaryFixed;
    type Unsafe = MarketOrdersSnapshotMessageBoundaryFixedUnsafe;

    fn symbol_id(&self) -> u32 {
        if self.is_out_of_bounds(8) {
            0u32.to_le()
        } else {
            u32::from_le(self.symbol_id)
        }
    }

    fn message_boundary(&self) -> MessageSetBoundaryEnum {
        if self.is_out_of_bounds(9) {
            MessageSetBoundaryEnum::MessageSetBoundaryUnset.to_le()
        } else {
            MessageSetBoundaryEnum::from_le(self.message_boundary)
        }
    }

    fn set_symbol_id(&mut self, value: u32) -> &mut Self {
        if !self.is_out_of_bounds(8) {
            self.symbol_id = value.to_le();
        }
        self
    }

    fn set_message_boundary(&mut self, value: MessageSetBoundaryEnum) -> &mut Self {
        if !self.is_out_of_bounds(9) {
            self.message_boundary = unsafe { core::mem::transmute((value as u8).to_le()) };
        }
        self
    }

    #[inline]
    fn clone_safe(&self) -> Self::Safe {
        let mut s = Self::Safe::new();
        self.copy_to(&mut s);
        s
    }

    #[inline]
    fn to_safe(self) -> Self::Safe {
        let mut s = Self::Safe::new();
        self.copy_to(&mut s);
        s
    }
}

#[cfg(test)]
pub(crate) mod tests {
    use super::*;

    #[test]
    pub(crate) fn layout() {
        unsafe {
            assert_eq!(
                9usize,
                core::mem::size_of::<MarketOrdersSnapshotMessageBoundaryFixedData>(),
                "MarketOrdersSnapshotMessageBoundaryFixedData sizeof expected {:} but was {:}",
                9usize,
                core::mem::size_of::<MarketOrdersSnapshotMessageBoundaryFixedData>()
            );
            assert_eq!(
                9u16,
                MarketOrdersSnapshotMessageBoundaryFixed::new().size(),
                "MarketOrdersSnapshotMessageBoundaryFixed sizeof expected {:} but was {:}",
                9u16,
                MarketOrdersSnapshotMessageBoundaryFixed::new().size(),
            );
            assert_eq!(
                MARKET_ORDERS_SNAPSHOT_MESSAGE_BOUNDARY,
                MarketOrdersSnapshotMessageBoundaryFixed::new().r#type(),
                "MarketOrdersSnapshotMessageBoundaryFixed type expected {:} but was {:}",
                MARKET_ORDERS_SNAPSHOT_MESSAGE_BOUNDARY,
                MarketOrdersSnapshotMessageBoundaryFixed::new().r#type(),
            );
            assert_eq!(
                155u16,
                MarketOrdersSnapshotMessageBoundaryFixed::new().r#type(),
                "MarketOrdersSnapshotMessageBoundaryFixed type expected {:} but was {:}",
                155u16,
                MarketOrdersSnapshotMessageBoundaryFixed::new().r#type(),
            );
            let d = MarketOrdersSnapshotMessageBoundaryFixedData::new();
            let p = (&d as *const _ as *const u8).offset(0) as usize;
            assert_eq!(
                0usize,
                (core::ptr::addr_of!(d.size) as usize) - p,
                "size offset expected {:} but was {:}",
                0usize,
                (core::ptr::addr_of!(d.size) as usize) - p,
            );
            assert_eq!(
                2usize,
                (core::ptr::addr_of!(d.r#type) as usize) - p,
                "type offset expected {:} but was {:}",
                2usize,
                (core::ptr::addr_of!(d.r#type) as usize) - p,
            );
            assert_eq!(
                4usize,
                (core::ptr::addr_of!(d.symbol_id) as usize) - p,
                "symbol_id offset expected {:} but was {:}",
                4usize,
                (core::ptr::addr_of!(d.symbol_id) as usize) - p,
            );
            assert_eq!(
                8usize,
                (core::ptr::addr_of!(d.message_boundary) as usize) - p,
                "message_boundary offset expected {:} but was {:}",
                8usize,
                (core::ptr::addr_of!(d.message_boundary) as usize) - p,
            );
        }
    }
}
