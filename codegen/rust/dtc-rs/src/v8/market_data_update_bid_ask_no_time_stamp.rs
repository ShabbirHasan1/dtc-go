// generated by github.com/moontrade/dtc-go/codegen/go at 2022-05-27 08:32:53.747629 +0800 WITA m=+0.008238335
use super::*;

pub(crate) const MARKET_DATA_UPDATE_BID_ASK_NO_TIME_STAMP_FIXED_SIZE: usize = 24;

/// size          u16  = MarketDataUpdateBidAskNoTimeStampFixedSize  (24)
/// type          u16  = MARKET_DATA_UPDATE_BID_ASK_NO_TIMESTAMP  (143)
/// symbol_id     u32  = 0
/// bid_price     f32  = f32::MAX
/// bid_quantity  u32  = 0
/// ask_price     f32  = f32::MAX
/// ask_quantity  u32  = 0
pub(crate) const MARKET_DATA_UPDATE_BID_ASK_NO_TIME_STAMP_FIXED_DEFAULT: [u8; 24] = [
    24, 0, 143, 0, 0, 0, 0, 0, 255, 255, 127, 127, 0, 0, 0, 0, 255, 255, 127, 127, 0, 0, 0, 0,
];

/// This message is optional.
///
/// Sent by the Server to the Client when there is an update to the Bid Ask
/// prices and/or quantities. This message is identical to the MarketDataUpdateBidAskFixed
/// message except it does not have a timestamp. It needs to be sent when
/// there is no change with the timestamp for the Bid Ask update as compared
/// to the prior update.
///
/// When the Server sends this message to the Client, the Client needs to
/// use the prior received Bid Ask update timestamp to know what the timestamp
/// is for this message.
pub trait MarketDataUpdateBidAskNoTimeStamp: Message {
    type Safe: MarketDataUpdateBidAskNoTimeStamp;
    type Unsafe: MarketDataUpdateBidAskNoTimeStamp;

    /// This is the same SymbolID sent by the Client in the MarketDataRequestVLS
    /// message which corresponds to the Symbol that the data in this message
    /// is for.
    fn symbol_id(&self) -> u32;

    /// The current Bid price. Leave unset if there is no price available.
    fn bid_price(&self) -> f32;

    /// The current number of contracts/shares at the Bid price.
    fn bid_quantity(&self) -> u32;

    /// The current Ask or offer price. Leave unset if there is no price available.
    /// The current Ask or offer price. Leave unset if there is no price available.
    fn ask_price(&self) -> f32;

    /// The current number of contracts/shares at the Ask price.
    fn ask_quantity(&self) -> u32;

    /// This is the same SymbolID sent by the Client in the MarketDataRequestVLS
    /// message which corresponds to the Symbol that the data in this message
    /// is for.
    fn set_symbol_id(&mut self, value: u32) -> &mut Self;

    /// The current Bid price. Leave unset if there is no price available.
    fn set_bid_price(&mut self, value: f32) -> &mut Self;

    /// The current number of contracts/shares at the Bid price.
    fn set_bid_quantity(&mut self, value: u32) -> &mut Self;

    /// The current Ask or offer price. Leave unset if there is no price available.
    /// The current Ask or offer price. Leave unset if there is no price available.
    fn set_ask_price(&mut self, value: f32) -> &mut Self;

    /// The current number of contracts/shares at the Ask price.
    fn set_ask_quantity(&mut self, value: u32) -> &mut Self;

    fn clone_safe(&self) -> Self::Safe;

    fn to_safe(self) -> Self::Safe;

    fn copy_to(&self, to: &mut impl MarketDataUpdateBidAskNoTimeStamp) {
        to.set_symbol_id(self.symbol_id());
        to.set_bid_price(self.bid_price());
        to.set_bid_quantity(self.bid_quantity());
        to.set_ask_price(self.ask_price());
        to.set_ask_quantity(self.ask_quantity());
    }

    #[inline]
    fn parse<F: Fn(Parsed<Self::Safe, Self::Unsafe>) -> Result<(), crate::Error>>(
        data: &[u8],
        f: F,
    ) -> Result<(), crate::Error> {
        if data.len() < 4 {
            return Err(crate::Error::Malformed("need more data"));
        }
        let size = unsafe { u16::from_le(*(data.as_ptr() as *const u16)) };
        let base_size = if Self::BASE_SIZE_OFFSET == 0 {
            size
        } else {
            let base_size = unsafe {
                u16::from_le(*(data.as_ptr().offset(Self::BASE_SIZE_OFFSET) as *const u16))
            };
            if base_size > size {
                return Err(crate::Error::Malformed("base_size is greater than size"));
            }
            base_size
        };
        if (base_size as usize) >= Self::BASE_SIZE {
            let msg = unsafe { Self::Safe::from_raw_parts(data.as_ptr(), size as usize) };
            let r = f(Parsed::Safe(&msg));
            core::mem::forget(msg);
            r
        } else {
            let msg = unsafe { Self::Unsafe::from_raw_parts(data.as_ptr(), size as usize) };
            let r = f(Parsed::Unsafe(&msg));
            core::mem::forget(msg);
            r
        }
    }
}

/// This message is optional.
///
/// Sent by the Server to the Client when there is an update to the Bid Ask
/// prices and/or quantities. This message is identical to the MarketDataUpdateBidAskFixed
/// message except it does not have a timestamp. It needs to be sent when
/// there is no change with the timestamp for the Bid Ask update as compared
/// to the prior update.
///
/// When the Server sends this message to the Client, the Client needs to
/// use the prior received Bid Ask update timestamp to know what the timestamp
/// is for this message.
pub struct MarketDataUpdateBidAskNoTimeStampFixed {
    data: *const MarketDataUpdateBidAskNoTimeStampFixedData,
}

pub struct MarketDataUpdateBidAskNoTimeStampFixedUnsafe {
    data: *const MarketDataUpdateBidAskNoTimeStampFixedData,
}

#[repr(packed(1), C)]
pub struct MarketDataUpdateBidAskNoTimeStampFixedData {
    size: u16,
    r#type: u16,
    symbol_id: u32,
    bid_price: f32,
    bid_quantity: u32,
    ask_price: f32,
    ask_quantity: u32,
}

impl MarketDataUpdateBidAskNoTimeStampFixedData {
    pub fn new() -> Self {
        Self {
            size: 24u16.to_le(),
            r#type: MARKET_DATA_UPDATE_BID_ASK_NO_TIMESTAMP.to_le(),
            symbol_id: 0u32,
            bid_price: f32_le(f32::MAX),
            bid_quantity: 0u32,
            ask_price: f32_le(f32::MAX),
            ask_quantity: 0u32,
        }
    }
}

unsafe impl Send for MarketDataUpdateBidAskNoTimeStampFixed {}
unsafe impl Send for MarketDataUpdateBidAskNoTimeStampFixedUnsafe {}
unsafe impl Send for MarketDataUpdateBidAskNoTimeStampFixedData {}

impl Drop for MarketDataUpdateBidAskNoTimeStampFixed {
    #[inline]
    fn drop(&mut self) {
        crate::deallocate(self.data as *mut u8, self.capacity() as usize);
    }
}

impl Drop for MarketDataUpdateBidAskNoTimeStampFixedUnsafe {
    #[inline]
    fn drop(&mut self) {
        crate::deallocate(self.data as *mut u8, self.capacity() as usize);
    }
}

impl Clone for MarketDataUpdateBidAskNoTimeStampFixed {
    #[inline]
    fn clone(&self) -> Self {
        let mut c = Self::new();
        self.copy_to(&mut c);
        c
    }
}

impl Clone for MarketDataUpdateBidAskNoTimeStampFixedUnsafe {
    #[inline]
    fn clone(&self) -> Self {
        let mut c = Self::new();
        self.copy_to(&mut c);
        c
    }
}

impl Into<Vec<u8>> for MarketDataUpdateBidAskNoTimeStampFixed {
    #[inline]
    fn into(self) -> Vec<u8> {
        self.into_vec()
    }
}

impl Into<Vec<u8>> for MarketDataUpdateBidAskNoTimeStampFixedUnsafe {
    #[inline]
    fn into(self) -> Vec<u8> {
        self.into_vec()
    }
}

impl core::ops::Deref for MarketDataUpdateBidAskNoTimeStampFixed {
    type Target = MarketDataUpdateBidAskNoTimeStampFixedData;

    #[inline]
    fn deref(&self) -> &Self::Target {
        unsafe { &*self.data }
    }
}

impl core::ops::DerefMut for MarketDataUpdateBidAskNoTimeStampFixed {
    #[inline]
    fn deref_mut(&mut self) -> &mut Self::Target {
        unsafe { &mut *(self.data as *mut Self::Target) }
    }
}

impl core::ops::Deref for MarketDataUpdateBidAskNoTimeStampFixedUnsafe {
    type Target = MarketDataUpdateBidAskNoTimeStampFixedData;

    #[inline]
    fn deref(&self) -> &Self::Target {
        unsafe { &*self.data }
    }
}

impl core::ops::DerefMut for MarketDataUpdateBidAskNoTimeStampFixedUnsafe {
    #[inline]
    fn deref_mut(&mut self) -> &mut Self::Target {
        unsafe { &mut *(self.data as *mut Self::Target) }
    }
}

impl core::fmt::Display for MarketDataUpdateBidAskNoTimeStampFixed {
    #[inline]
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(format!("MarketDataUpdateBidAskNoTimeStampFixed(size: {}, type: {}, symbol_id: {}, bid_price: {}, bid_quantity: {}, ask_price: {}, ask_quantity: {})", self.size(), self.r#type(), self.symbol_id(), self.bid_price(), self.bid_quantity(), self.ask_price(), self.ask_quantity()).as_str())
    }
}

impl core::fmt::Debug for MarketDataUpdateBidAskNoTimeStampFixed {
    #[inline]
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(format!("MarketDataUpdateBidAskNoTimeStampFixed(size: {}, type: {}, symbol_id: {}, bid_price: {}, bid_quantity: {}, ask_price: {}, ask_quantity: {})", self.size(), self.r#type(), self.symbol_id(), self.bid_price(), self.bid_quantity(), self.ask_price(), self.ask_quantity()).as_str())
    }
}

impl core::fmt::Display for MarketDataUpdateBidAskNoTimeStampFixedUnsafe {
    #[inline]
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(format!("MarketDataUpdateBidAskNoTimeStampFixedUnsafe(size: {}, type: {}, symbol_id: {}, bid_price: {}, bid_quantity: {}, ask_price: {}, ask_quantity: {})", self.size(), self.r#type(), self.symbol_id(), self.bid_price(), self.bid_quantity(), self.ask_price(), self.ask_quantity()).as_str())
    }
}

impl core::fmt::Debug for MarketDataUpdateBidAskNoTimeStampFixedUnsafe {
    #[inline]
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(format!("MarketDataUpdateBidAskNoTimeStampFixedUnsafe(size: {}, type: {}, symbol_id: {}, bid_price: {}, bid_quantity: {}, ask_price: {}, ask_quantity: {})", self.size(), self.r#type(), self.symbol_id(), self.bid_price(), self.bid_quantity(), self.ask_price(), self.ask_quantity()).as_str())
    }
}

impl crate::Message for MarketDataUpdateBidAskNoTimeStampFixed {
    type Data = MarketDataUpdateBidAskNoTimeStampFixedData;

    const TYPE: u16 = MARKET_DATA_UPDATE_BID_ASK_NO_TIMESTAMP;
    const BASE_SIZE: usize = 24;
    const BASE_SIZE_OFFSET: isize = 0;

    #[inline]
    fn new() -> Self {
        Self {
            data: crate::allocate(
                Self::BASE_SIZE,
                MarketDataUpdateBidAskNoTimeStampFixedData::new(),
            ),
        }
    }

    #[inline]
    fn size(&self) -> u16 {
        u16::from_le(self.size)
    }

    #[inline]
    fn r#type(&self) -> u16 {
        u16::from_le(self.r#type)
    }

    #[inline]
    fn base_size(&self) -> u16 {
        u16::from_le(self.size)
    }

    #[inline]
    fn capacity(&self) -> u16 {
        u16::from_le(self.size)
    }

    #[inline]
    unsafe fn as_ptr(&self) -> *const Self::Data {
        self.data
    }

    #[inline]
    unsafe fn from_raw_parts(data: *const u8, _: usize) -> Self {
        Self {
            data: data as *const MarketDataUpdateBidAskNoTimeStampFixedData,
        }
    }
}
impl crate::Message for MarketDataUpdateBidAskNoTimeStampFixedUnsafe {
    type Data = MarketDataUpdateBidAskNoTimeStampFixedData;

    const TYPE: u16 = MARKET_DATA_UPDATE_BID_ASK_NO_TIMESTAMP;
    const BASE_SIZE: usize = 24;
    const BASE_SIZE_OFFSET: isize = 0;

    #[inline]
    fn new() -> Self {
        Self {
            data: crate::allocate(
                Self::BASE_SIZE,
                MarketDataUpdateBidAskNoTimeStampFixedData::new(),
            ),
        }
    }

    #[inline]
    fn size(&self) -> u16 {
        u16::from_le(self.size)
    }

    #[inline]
    fn r#type(&self) -> u16 {
        u16::from_le(self.r#type)
    }

    #[inline]
    fn base_size(&self) -> u16 {
        u16::from_le(self.size)
    }

    #[inline]
    fn capacity(&self) -> u16 {
        u16::from_le(self.size)
    }

    #[inline]
    unsafe fn as_ptr(&self) -> *const Self::Data {
        self.data
    }

    #[inline]
    unsafe fn from_raw_parts(data: *const u8, _: usize) -> Self {
        Self {
            data: data as *const MarketDataUpdateBidAskNoTimeStampFixedData,
        }
    }
}
/// This message is optional.
///
/// Sent by the Server to the Client when there is an update to the Bid Ask
/// prices and/or quantities. This message is identical to the MarketDataUpdateBidAskFixed
/// message except it does not have a timestamp. It needs to be sent when
/// there is no change with the timestamp for the Bid Ask update as compared
/// to the prior update.
///
/// When the Server sends this message to the Client, the Client needs to
/// use the prior received Bid Ask update timestamp to know what the timestamp
/// is for this message.
impl MarketDataUpdateBidAskNoTimeStamp for MarketDataUpdateBidAskNoTimeStampFixed {
    type Safe = MarketDataUpdateBidAskNoTimeStampFixed;
    type Unsafe = MarketDataUpdateBidAskNoTimeStampFixedUnsafe;

    /// This is the same SymbolID sent by the Client in the MarketDataRequestVLS
    /// message which corresponds to the Symbol that the data in this message
    /// is for.
    fn symbol_id(&self) -> u32 {
        u32::from_le(self.symbol_id)
    }

    /// The current Bid price. Leave unset if there is no price available.
    fn bid_price(&self) -> f32 {
        f32_le(self.bid_price)
    }

    /// The current number of contracts/shares at the Bid price.
    fn bid_quantity(&self) -> u32 {
        u32::from_le(self.bid_quantity)
    }

    /// The current Ask or offer price. Leave unset if there is no price available.
    /// The current Ask or offer price. Leave unset if there is no price available.
    fn ask_price(&self) -> f32 {
        f32_le(self.ask_price)
    }

    /// The current number of contracts/shares at the Ask price.
    fn ask_quantity(&self) -> u32 {
        u32::from_le(self.ask_quantity)
    }

    /// This is the same SymbolID sent by the Client in the MarketDataRequestVLS
    /// message which corresponds to the Symbol that the data in this message
    /// is for.
    fn set_symbol_id(&mut self, value: u32) -> &mut Self {
        self.symbol_id = value.to_le();
        self
    }

    /// The current Bid price. Leave unset if there is no price available.
    fn set_bid_price(&mut self, value: f32) -> &mut Self {
        self.bid_price = f32_le(value);
        self
    }

    /// The current number of contracts/shares at the Bid price.
    fn set_bid_quantity(&mut self, value: u32) -> &mut Self {
        self.bid_quantity = value.to_le();
        self
    }

    /// The current Ask or offer price. Leave unset if there is no price available.
    /// The current Ask or offer price. Leave unset if there is no price available.
    fn set_ask_price(&mut self, value: f32) -> &mut Self {
        self.ask_price = f32_le(value);
        self
    }

    /// The current number of contracts/shares at the Ask price.
    fn set_ask_quantity(&mut self, value: u32) -> &mut Self {
        self.ask_quantity = value.to_le();
        self
    }

    #[inline]
    fn clone_safe(&self) -> Self::Safe {
        let mut s = Self::Safe::new();
        self.copy_to(&mut s);
        s
    }

    #[inline]
    fn to_safe(self) -> Self::Safe {
        self
    }
}

/// This message is optional.
///
/// Sent by the Server to the Client when there is an update to the Bid Ask
/// prices and/or quantities. This message is identical to the MarketDataUpdateBidAskFixed
/// message except it does not have a timestamp. It needs to be sent when
/// there is no change with the timestamp for the Bid Ask update as compared
/// to the prior update.
///
/// When the Server sends this message to the Client, the Client needs to
/// use the prior received Bid Ask update timestamp to know what the timestamp
/// is for this message.
impl MarketDataUpdateBidAskNoTimeStamp for MarketDataUpdateBidAskNoTimeStampFixedUnsafe {
    type Safe = MarketDataUpdateBidAskNoTimeStampFixed;
    type Unsafe = MarketDataUpdateBidAskNoTimeStampFixedUnsafe;

    /// This is the same SymbolID sent by the Client in the MarketDataRequestVLS
    /// message which corresponds to the Symbol that the data in this message
    /// is for.
    fn symbol_id(&self) -> u32 {
        if self.is_out_of_bounds(8) {
            0u32
        } else {
            u32::from_le(self.symbol_id)
        }
    }

    /// The current Bid price. Leave unset if there is no price available.
    fn bid_price(&self) -> f32 {
        if self.is_out_of_bounds(12) {
            f32_le(f32::MAX)
        } else {
            f32_le(self.bid_price)
        }
    }

    /// The current number of contracts/shares at the Bid price.
    fn bid_quantity(&self) -> u32 {
        if self.is_out_of_bounds(16) {
            0u32
        } else {
            u32::from_le(self.bid_quantity)
        }
    }

    /// The current Ask or offer price. Leave unset if there is no price available.
    /// The current Ask or offer price. Leave unset if there is no price available.
    fn ask_price(&self) -> f32 {
        if self.is_out_of_bounds(20) {
            f32_le(f32::MAX)
        } else {
            f32_le(self.ask_price)
        }
    }

    /// The current number of contracts/shares at the Ask price.
    fn ask_quantity(&self) -> u32 {
        if self.is_out_of_bounds(24) {
            0u32
        } else {
            u32::from_le(self.ask_quantity)
        }
    }

    /// This is the same SymbolID sent by the Client in the MarketDataRequestVLS
    /// message which corresponds to the Symbol that the data in this message
    /// is for.
    fn set_symbol_id(&mut self, value: u32) -> &mut Self {
        if !self.is_out_of_bounds(8) {
            self.symbol_id = value.to_le();
        }
        self
    }

    /// The current Bid price. Leave unset if there is no price available.
    fn set_bid_price(&mut self, value: f32) -> &mut Self {
        if !self.is_out_of_bounds(12) {
            self.bid_price = f32_le(value);
        }
        self
    }

    /// The current number of contracts/shares at the Bid price.
    fn set_bid_quantity(&mut self, value: u32) -> &mut Self {
        if !self.is_out_of_bounds(16) {
            self.bid_quantity = value.to_le();
        }
        self
    }

    /// The current Ask or offer price. Leave unset if there is no price available.
    /// The current Ask or offer price. Leave unset if there is no price available.
    fn set_ask_price(&mut self, value: f32) -> &mut Self {
        if !self.is_out_of_bounds(20) {
            self.ask_price = f32_le(value);
        }
        self
    }

    /// The current number of contracts/shares at the Ask price.
    fn set_ask_quantity(&mut self, value: u32) -> &mut Self {
        if !self.is_out_of_bounds(24) {
            self.ask_quantity = value.to_le();
        }
        self
    }

    #[inline]
    fn clone_safe(&self) -> Self::Safe {
        let mut s = Self::Safe::new();
        self.copy_to(&mut s);
        s
    }

    #[inline]
    fn to_safe(self) -> Self::Safe {
        let mut s = Self::Safe::new();
        self.copy_to(&mut s);
        s
    }
}

#[cfg(test)]
pub(crate) mod tests {
    use super::*;

    #[test]
    pub(crate) fn layout() {
        unsafe {
            assert_eq!(
                24usize,
                core::mem::size_of::<MarketDataUpdateBidAskNoTimeStampFixedData>(),
                "MarketDataUpdateBidAskNoTimeStampFixedData sizeof expected {:} but was {:}",
                24usize,
                core::mem::size_of::<MarketDataUpdateBidAskNoTimeStampFixedData>()
            );
            assert_eq!(
                24u16,
                MarketDataUpdateBidAskNoTimeStampFixed::new().size(),
                "MarketDataUpdateBidAskNoTimeStampFixed sizeof expected {:} but was {:}",
                24u16,
                MarketDataUpdateBidAskNoTimeStampFixed::new().size(),
            );
            assert_eq!(
                MARKET_DATA_UPDATE_BID_ASK_NO_TIMESTAMP,
                MarketDataUpdateBidAskNoTimeStampFixed::new().r#type(),
                "MarketDataUpdateBidAskNoTimeStampFixed type expected {:} but was {:}",
                MARKET_DATA_UPDATE_BID_ASK_NO_TIMESTAMP,
                MarketDataUpdateBidAskNoTimeStampFixed::new().r#type(),
            );
            assert_eq!(
                143u16,
                MarketDataUpdateBidAskNoTimeStampFixed::new().r#type(),
                "MarketDataUpdateBidAskNoTimeStampFixed type expected {:} but was {:}",
                143u16,
                MarketDataUpdateBidAskNoTimeStampFixed::new().r#type(),
            );
            let d = MarketDataUpdateBidAskNoTimeStampFixedData::new();
            let p = (&d as *const _ as *const u8).offset(0) as usize;
            assert_eq!(
                0usize,
                (core::ptr::addr_of!(d.size) as usize) - p,
                "size offset expected {:} but was {:}",
                0usize,
                (core::ptr::addr_of!(d.size) as usize) - p,
            );
            assert_eq!(
                2usize,
                (core::ptr::addr_of!(d.r#type) as usize) - p,
                "type offset expected {:} but was {:}",
                2usize,
                (core::ptr::addr_of!(d.r#type) as usize) - p,
            );
            assert_eq!(
                4usize,
                (core::ptr::addr_of!(d.symbol_id) as usize) - p,
                "symbol_id offset expected {:} but was {:}",
                4usize,
                (core::ptr::addr_of!(d.symbol_id) as usize) - p,
            );
            assert_eq!(
                8usize,
                (core::ptr::addr_of!(d.bid_price) as usize) - p,
                "bid_price offset expected {:} but was {:}",
                8usize,
                (core::ptr::addr_of!(d.bid_price) as usize) - p,
            );
            assert_eq!(
                12usize,
                (core::ptr::addr_of!(d.bid_quantity) as usize) - p,
                "bid_quantity offset expected {:} but was {:}",
                12usize,
                (core::ptr::addr_of!(d.bid_quantity) as usize) - p,
            );
            assert_eq!(
                16usize,
                (core::ptr::addr_of!(d.ask_price) as usize) - p,
                "ask_price offset expected {:} but was {:}",
                16usize,
                (core::ptr::addr_of!(d.ask_price) as usize) - p,
            );
            assert_eq!(
                20usize,
                (core::ptr::addr_of!(d.ask_quantity) as usize) - p,
                "ask_quantity offset expected {:} but was {:}",
                20usize,
                (core::ptr::addr_of!(d.ask_quantity) as usize) - p,
            );
        }
    }
}
