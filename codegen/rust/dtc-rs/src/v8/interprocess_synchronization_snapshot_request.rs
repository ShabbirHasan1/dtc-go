// generated by github.com/moontrade/dtc-go/codegen/go at 2022-05-27 08:32:53.747629 +0800 WITA m=+0.008238335
use super::*;

pub(crate) const INTERPROCESS_SYNCHRONIZATION_SNAPSHOT_REQUEST_FIXED_SIZE: usize = 8;

/// size        u16  = InterprocessSynchronizationSnapshotRequestFixedSize  (8)
/// type        u16  = INTERPROCESS_SYNCHRONIZATION_SNAPSHOT_REQUEST  (10133)
/// request_id  u32  = 0
pub(crate) const INTERPROCESS_SYNCHRONIZATION_SNAPSHOT_REQUEST_FIXED_DEFAULT: [u8; 8] =
    [8, 0, 149, 39, 0, 0, 0, 0];

pub trait InterprocessSynchronizationSnapshotRequest: Message {
    type Safe: InterprocessSynchronizationSnapshotRequest;
    type Unsafe: InterprocessSynchronizationSnapshotRequest;

    fn request_id(&self) -> u32;

    fn set_request_id(&mut self, value: u32) -> &mut Self;

    fn clone_safe(&self) -> Self::Safe;

    fn to_safe(self) -> Self::Safe;

    fn copy_to(&self, to: &mut impl InterprocessSynchronizationSnapshotRequest) {
        to.set_request_id(self.request_id());
    }

    #[inline]
    fn parse<F: Fn(Parsed<Self::Safe, Self::Unsafe>) -> Result<(), crate::Error>>(
        data: &[u8],
        f: F,
    ) -> Result<(), crate::Error> {
        if data.len() < 4 {
            return Err(crate::Error::Malformed("need more data"));
        }
        let size = unsafe { u16::from_le(*(data.as_ptr() as *const u16)) };
        let base_size = if Self::BASE_SIZE_OFFSET == 0 {
            size
        } else {
            let base_size = unsafe {
                u16::from_le(*(data.as_ptr().offset(Self::BASE_SIZE_OFFSET) as *const u16))
            };
            if base_size > size {
                return Err(crate::Error::Malformed("base_size is greater than size"));
            }
            base_size
        };
        if (base_size as usize) >= Self::BASE_SIZE {
            let msg = unsafe { Self::Safe::from_raw_parts(data.as_ptr(), size as usize) };
            let r = f(Parsed::Safe(&msg));
            core::mem::forget(msg);
            r
        } else {
            let msg = unsafe { Self::Unsafe::from_raw_parts(data.as_ptr(), size as usize) };
            let r = f(Parsed::Unsafe(&msg));
            core::mem::forget(msg);
            r
        }
    }
}

pub struct InterprocessSynchronizationSnapshotRequestFixed {
    data: *const InterprocessSynchronizationSnapshotRequestFixedData,
}

pub struct InterprocessSynchronizationSnapshotRequestFixedUnsafe {
    data: *const InterprocessSynchronizationSnapshotRequestFixedData,
}

#[repr(packed(1), C)]
pub struct InterprocessSynchronizationSnapshotRequestFixedData {
    size: u16,
    r#type: u16,
    request_id: u32,
}

impl InterprocessSynchronizationSnapshotRequestFixedData {
    pub fn new() -> Self {
        Self {
            size: 8u16.to_le(),
            r#type: INTERPROCESS_SYNCHRONIZATION_SNAPSHOT_REQUEST.to_le(),
            request_id: 0u32.to_le(),
        }
    }
}

unsafe impl Send for InterprocessSynchronizationSnapshotRequestFixed {}
unsafe impl Send for InterprocessSynchronizationSnapshotRequestFixedUnsafe {}
unsafe impl Send for InterprocessSynchronizationSnapshotRequestFixedData {}

impl Drop for InterprocessSynchronizationSnapshotRequestFixed {
    #[inline]
    fn drop(&mut self) {
        crate::deallocate(self.data as *mut u8, self.capacity() as usize);
    }
}

impl Drop for InterprocessSynchronizationSnapshotRequestFixedUnsafe {
    #[inline]
    fn drop(&mut self) {
        crate::deallocate(self.data as *mut u8, self.capacity() as usize);
    }
}

impl Clone for InterprocessSynchronizationSnapshotRequestFixed {
    #[inline]
    fn clone(&self) -> Self {
        let mut c = Self::new();
        self.copy_to(&mut c);
        c
    }
}

impl Clone for InterprocessSynchronizationSnapshotRequestFixedUnsafe {
    #[inline]
    fn clone(&self) -> Self {
        let mut c = Self::new();
        self.copy_to(&mut c);
        c
    }
}

impl Into<Vec<u8>> for InterprocessSynchronizationSnapshotRequestFixed {
    #[inline]
    fn into(self) -> Vec<u8> {
        self.into_vec()
    }
}

impl Into<Vec<u8>> for InterprocessSynchronizationSnapshotRequestFixedUnsafe {
    #[inline]
    fn into(self) -> Vec<u8> {
        self.into_vec()
    }
}

impl core::ops::Deref for InterprocessSynchronizationSnapshotRequestFixed {
    type Target = InterprocessSynchronizationSnapshotRequestFixedData;

    #[inline]
    fn deref(&self) -> &Self::Target {
        unsafe { &*self.data }
    }
}

impl core::ops::DerefMut for InterprocessSynchronizationSnapshotRequestFixed {
    #[inline]
    fn deref_mut(&mut self) -> &mut Self::Target {
        unsafe { &mut *(self.data as *mut Self::Target) }
    }
}

impl core::ops::Deref for InterprocessSynchronizationSnapshotRequestFixedUnsafe {
    type Target = InterprocessSynchronizationSnapshotRequestFixedData;

    #[inline]
    fn deref(&self) -> &Self::Target {
        unsafe { &*self.data }
    }
}

impl core::ops::DerefMut for InterprocessSynchronizationSnapshotRequestFixedUnsafe {
    #[inline]
    fn deref_mut(&mut self) -> &mut Self::Target {
        unsafe { &mut *(self.data as *mut Self::Target) }
    }
}

impl core::fmt::Display for InterprocessSynchronizationSnapshotRequestFixed {
    #[inline]
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(format!("InterprocessSynchronizationSnapshotRequestFixed(size: {}, type: {}, request_id: {})", self.size(), self.r#type(), self.request_id()).as_str())
    }
}

impl core::fmt::Debug for InterprocessSynchronizationSnapshotRequestFixed {
    #[inline]
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(format!("InterprocessSynchronizationSnapshotRequestFixed(size: {}, type: {}, request_id: {})", self.size(), self.r#type(), self.request_id()).as_str())
    }
}

impl core::fmt::Display for InterprocessSynchronizationSnapshotRequestFixedUnsafe {
    #[inline]
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(format!("InterprocessSynchronizationSnapshotRequestFixedUnsafe(size: {}, type: {}, request_id: {})", self.size(), self.r#type(), self.request_id()).as_str())
    }
}

impl core::fmt::Debug for InterprocessSynchronizationSnapshotRequestFixedUnsafe {
    #[inline]
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(format!("InterprocessSynchronizationSnapshotRequestFixedUnsafe(size: {}, type: {}, request_id: {})", self.size(), self.r#type(), self.request_id()).as_str())
    }
}

impl crate::Message for InterprocessSynchronizationSnapshotRequestFixed {
    type Data = InterprocessSynchronizationSnapshotRequestFixedData;

    const TYPE: u16 = INTERPROCESS_SYNCHRONIZATION_SNAPSHOT_REQUEST;
    const BASE_SIZE: usize = 8;
    const BASE_SIZE_OFFSET: isize = 0;

    #[inline]
    fn new() -> Self {
        Self {
            data: crate::allocate(
                Self::BASE_SIZE,
                InterprocessSynchronizationSnapshotRequestFixedData::new(),
            ),
        }
    }

    #[inline]
    fn size(&self) -> u16 {
        u16::from_le(self.size)
    }

    #[inline]
    fn r#type(&self) -> u16 {
        u16::from_le(self.r#type)
    }

    #[inline]
    fn base_size(&self) -> u16 {
        u16::from_le(self.size)
    }

    #[inline]
    fn capacity(&self) -> u16 {
        u16::from_le(self.size)
    }

    #[inline]
    unsafe fn as_ptr(&self) -> *const Self::Data {
        self.data
    }

    #[inline]
    unsafe fn from_raw_parts(data: *const u8, _: usize) -> Self {
        Self {
            data: data as *const InterprocessSynchronizationSnapshotRequestFixedData,
        }
    }
}
impl crate::Message for InterprocessSynchronizationSnapshotRequestFixedUnsafe {
    type Data = InterprocessSynchronizationSnapshotRequestFixedData;

    const TYPE: u16 = INTERPROCESS_SYNCHRONIZATION_SNAPSHOT_REQUEST;
    const BASE_SIZE: usize = 8;
    const BASE_SIZE_OFFSET: isize = 0;

    #[inline]
    fn new() -> Self {
        Self {
            data: crate::allocate(
                Self::BASE_SIZE,
                InterprocessSynchronizationSnapshotRequestFixedData::new(),
            ),
        }
    }

    #[inline]
    fn size(&self) -> u16 {
        u16::from_le(self.size)
    }

    #[inline]
    fn r#type(&self) -> u16 {
        u16::from_le(self.r#type)
    }

    #[inline]
    fn base_size(&self) -> u16 {
        u16::from_le(self.size)
    }

    #[inline]
    fn capacity(&self) -> u16 {
        u16::from_le(self.size)
    }

    #[inline]
    unsafe fn as_ptr(&self) -> *const Self::Data {
        self.data
    }

    #[inline]
    unsafe fn from_raw_parts(data: *const u8, _: usize) -> Self {
        Self {
            data: data as *const InterprocessSynchronizationSnapshotRequestFixedData,
        }
    }
}
impl InterprocessSynchronizationSnapshotRequest
    for InterprocessSynchronizationSnapshotRequestFixed
{
    type Safe = InterprocessSynchronizationSnapshotRequestFixed;
    type Unsafe = InterprocessSynchronizationSnapshotRequestFixedUnsafe;

    fn request_id(&self) -> u32 {
        u32::from_le(self.request_id)
    }

    fn set_request_id(&mut self, value: u32) -> &mut Self {
        self.request_id = value.to_le();
        self
    }

    #[inline]
    fn clone_safe(&self) -> Self::Safe {
        let mut s = Self::Safe::new();
        self.copy_to(&mut s);
        s
    }

    #[inline]
    fn to_safe(self) -> Self::Safe {
        self
    }
}

impl InterprocessSynchronizationSnapshotRequest
    for InterprocessSynchronizationSnapshotRequestFixedUnsafe
{
    type Safe = InterprocessSynchronizationSnapshotRequestFixed;
    type Unsafe = InterprocessSynchronizationSnapshotRequestFixedUnsafe;

    fn request_id(&self) -> u32 {
        if self.is_out_of_bounds(8) {
            0u32.to_le()
        } else {
            u32::from_le(self.request_id)
        }
    }

    fn set_request_id(&mut self, value: u32) -> &mut Self {
        if !self.is_out_of_bounds(8) {
            self.request_id = value.to_le();
        }
        self
    }

    #[inline]
    fn clone_safe(&self) -> Self::Safe {
        let mut s = Self::Safe::new();
        self.copy_to(&mut s);
        s
    }

    #[inline]
    fn to_safe(self) -> Self::Safe {
        let mut s = Self::Safe::new();
        self.copy_to(&mut s);
        s
    }
}

#[cfg(test)]
pub(crate) mod tests {
    use super::*;

    #[test]
    pub(crate) fn layout() {
        unsafe {
            assert_eq!(
                8usize,
                core::mem::size_of::<InterprocessSynchronizationSnapshotRequestFixedData>(),
                "InterprocessSynchronizationSnapshotRequestFixedData sizeof expected {:} but was {:}",
                8usize,
                core::mem::size_of::<InterprocessSynchronizationSnapshotRequestFixedData>()
            );
            assert_eq!(
                8u16,
                InterprocessSynchronizationSnapshotRequestFixed::new().size(),
                "InterprocessSynchronizationSnapshotRequestFixed sizeof expected {:} but was {:}",
                8u16,
                InterprocessSynchronizationSnapshotRequestFixed::new().size(),
            );
            assert_eq!(
                INTERPROCESS_SYNCHRONIZATION_SNAPSHOT_REQUEST,
                InterprocessSynchronizationSnapshotRequestFixed::new().r#type(),
                "InterprocessSynchronizationSnapshotRequestFixed type expected {:} but was {:}",
                INTERPROCESS_SYNCHRONIZATION_SNAPSHOT_REQUEST,
                InterprocessSynchronizationSnapshotRequestFixed::new().r#type(),
            );
            assert_eq!(
                10133u16,
                InterprocessSynchronizationSnapshotRequestFixed::new().r#type(),
                "InterprocessSynchronizationSnapshotRequestFixed type expected {:} but was {:}",
                10133u16,
                InterprocessSynchronizationSnapshotRequestFixed::new().r#type(),
            );
            let d = InterprocessSynchronizationSnapshotRequestFixedData::new();
            let p = (&d as *const _ as *const u8).offset(0) as usize;
            assert_eq!(
                0usize,
                (core::ptr::addr_of!(d.size) as usize) - p,
                "size offset expected {:} but was {:}",
                0usize,
                (core::ptr::addr_of!(d.size) as usize) - p,
            );
            assert_eq!(
                2usize,
                (core::ptr::addr_of!(d.r#type) as usize) - p,
                "type offset expected {:} but was {:}",
                2usize,
                (core::ptr::addr_of!(d.r#type) as usize) - p,
            );
            assert_eq!(
                4usize,
                (core::ptr::addr_of!(d.request_id) as usize) - p,
                "request_id offset expected {:} but was {:}",
                4usize,
                (core::ptr::addr_of!(d.request_id) as usize) - p,
            );
        }
    }
}
